var documenterSearchIndex = {"docs":
[{"category":"section","location":"limitations/#Limitations-and-Legal-Notice","page":"Limitations","text":"warning: Not a substitute for independently validated metrology software\nSymbolicUncertainties.jl implements the mathematical methodology of JCGM 100:2008 (GUM). It is intended for research and educational use only and has not been independently validated for regulated or accreditation contexts.\n\nThis page consolidates the scope boundaries, waived requirements, performance expectations, and legal posture of SymbolicUncertainties.jl v0.10.0 (REQ-173).","title":"Limitations and Legal Notice"},{"category":"section","location":"limitations/#Non-warranty-posture-(REQ-170-/-REQ-172)","page":"Limitations","text":"SymbolicUncertainties.jl is distributed without any warranty — not even the implied warranty of merchantability or fitness for a particular purpose. The package paraphrases the methodology of JCGM 100:2008 in software; it does not reproduce the normative text, tables, or figures of that document, nor of any IEC or EA publication. Users in regulated contexts (legal metrology, ISO/IEC 17025 calibration laboratories, clinical measurement, safety-critical instrumentation) MUST independently verify every computed result against their accreditation body's requirements. Prohibited wording (per REQ-171) is absent from the source tree and checked on every CI run by test/package/test_banned_terms.jl. The verbatim clause required by REQ-170 lives in NOTICE.md at the repository root, kept separate from LICENSE.md so the licence text itself stays the unmodified, OSI-approved MIT Licence.","title":"Non-warranty posture (REQ-170 / REQ-172)"},{"category":"section","location":"limitations/#No-reproduction-audit-(REQ-176)","page":"Limitations","text":"The M10 release audit (2026-04-19) confirms that no normative text, table, or figure from JCGM 100:2008, JCGM 101:2008, JCGM 102:2011, JCGM 104:2009, IEC 60359, IEC 61010, IEC 61298-3, or EA-4/02 is reproduced in the source tree. Section numbers are cited in docstrings to indicate the method each function realises; the methodology itself is paraphrased, not copied. Citations live in CITATION.bib at the repository root.","title":"No-reproduction audit (REQ-176)"},{"category":"section","location":"limitations/#Waived-EARS-requirements","page":"Limitations","text":"The following requirements from specification/ears.md are explicitly deferred past v0.10.0, with rationale below. Each waiver is tracked in upstream-bugs.md or the ROADMAP post-1.0 candidate list.\n\nWaiver Rationale Tracking\nSecond-order corrections Out of scope — constitution III: purely symbolic first-order GUM. Second-order / mixed partials are a v2.0 opt-in. ROADMAP post-1.0 \"Second-order propagation behind an explicit opt-in flag\"\nFortranTarget code generation Blocked by upstream — Symbolics.jl 7.x does not export FortranTarget. C-target is the only non-Julia target at v0.10.0. upstream-bugs.md UB-004\nassume_positive! / assume_nonzero! Blocked by upstream — Symbolics.jl lacks a first-class assumptions API. M4 takes a conservative over-warning stance (REQ-140 / REQ-141). upstream-bugs.md UB-003\nMonte Carlo propagation (JCGM 101:2008) Out of scope — constitution III delegates to MonteCarloMeasurements.jl. A v1.x bridge is a candidate. ROADMAP post-1.0 \"Bridge to MonteCarloMeasurements.jl\"\nDAE support (M9 follow-up) Scope constraint — M9 targets ODEs only; DAE systems raise ArgumentError. Driven by user demand. ROADMAP M9 \"Deferred\"\nTime-varying uncertain parameters (M9) JCGM 101:2008 territory; stochastic forcing belongs to StochasticDiffEq.jl + MonteCarloMeasurements.jl. ROADMAP M9 \"Deferred\"\nCI-gated benchmarks (M7, M9, M10) Wall-clock measurements are flaky on shared CI runners — benchmarks documented only. M7 / M9 / M10 spec notes","title":"Waived EARS requirements"},{"category":"section","location":"limitations/#Performance-notes","page":"Limitations","text":"","title":"Performance notes"},{"category":"section","location":"limitations/#First-call-latency-(SC-001)","page":"Limitations","text":"With v0.10.0's PrecompileTools.@compile_workload block, the canonical arithmetic first call should complete in under 1 second on a standard developer laptop:\n\nusing SymbolicUncertainties\nusing Symbolics\n@variables V I σV σI\n\n@time (V ± σV) / (I ± σI)\n# elapsed: ~0.1–0.5 s on a warm-precompile run\n\nThis target is documented, not CI-gated — shared CI runners under neighbour load routinely exceed 1 second on cold startup, and a flaky gate would teach contributors to ignore failures. The precompile workload covers four call paths (arithmetic, math functions, propagate, uncertainty_budget) — roughly 95% of common user sessions. The M4/M5/M6/M7/M8/M9 paths are intentionally not in the precompile workload to keep package load time bounded.","title":"First-call latency (SC-001)"},{"category":"section","location":"limitations/#Type-stability","page":"Limitations","text":"A @code_warntype audit of the hot paths (SymbolicMeasurement arithmetic, propagate, uncertainty_budget) at v0.10.0 shows no Any or Union returns on the canonical call shapes. Extension paths (MTK, Latexify, DataFrames) inherit the upstream packages' type-stability posture.","title":"Type stability"},{"category":"section","location":"limitations/#Skipping-precompilation","page":"Limitations","text":"Users who want to skip the precompile workload (e.g. on CI layers that precompile once per PR) can set JULIA_PKG_PRECOMPILE_AUTO=0 before using SymbolicUncertainties. First-call latency will regress to pre-v0.10.0 levels, but functionality is unaffected.","title":"Skipping precompilation"},{"category":"section","location":"limitations/#Post-1.0-candidates","page":"Limitations","text":"See ROADMAP.md \"Post-1.0 candidates (unscheduled)\" for the full list. Highlights:\n\nBridge to MonteCarloMeasurements.jl for JCGM 101:2008 Monte Carlo cross-checks.\nSecond-order propagation behind an opt-in flag.\nRicher Student-t k computation for symbolic ν_eff.\nCertificate-quality PDF export from uncertainty_budget.\nMechanical / thermal / optical tutorial gallery beyond the six canonical electrical examples.\nDAE support in the ModelingToolkit extension (M9.x).\n~~Measurements.jl-style linear-form-in-tagged-sources representation~~ — no longer a post-1.0 candidate: scheduled as milestone M11 (v0.11.0), where it replaces the binary-operator path rather than supplementing it.\n\n","title":"Post-1.0 candidates"},{"category":"section","location":"limitations/#Known-limitations-(M1–M9-legacy)","page":"Limitations","text":"The remaining sections document specific limitations encountered during M1–M9 development. They are kept for traceability and user awareness.","title":"Known limitations (M1–M9 legacy)"},{"category":"section","location":"limitations/#Binary-operators-and-operand-identity-—-resolved-in-M11","page":"Limitations","text":"Until milestone M11 the five binary operators applied the uncorrelated GUM formula to each operand's uncertainty number, which could not know whether two operands were the same measurement. x - x therefore reported σ√2 rather than zero, and x + x reported σ√2 rather than 2σ.\n\nThis is fixed at the type level. A quantity now records which independent sources it derives from and with what sensitivity, and the operators combine those linear forms by the chain rule, so sensitivities of a shared source add — and cancel — before the variance is formed. See Uncertainty Sources.\n\nThe two workarounds this section used to document, and the upstream-bugs.md UB-002 entry, are withdrawn: they described a limitation that no longer exists.","title":"Binary operators and operand identity — resolved in M11"},{"category":"section","location":"limitations/#Linearity-assumption-(GUM-§5.1.1)","page":"Limitations","text":"The GUM framework assumes the measurement model function is approximately linear in the vicinity of the input estimates. When the model is strongly nonlinear, the first-order Taylor expansion that drives the combined standard uncertainty formula becomes inadequate, and higher-order terms or Monte Carlo propagation (JCGM 101:2008) should be considered. Use check_linearity to obtain per-variable nonlinearity indicators and a runtime @warn when the GUM §5.1.1 threshold |η| > 0.1 is exceeded.","title":"Linearity assumption (GUM §5.1.1)"},{"category":"section","location":"limitations/#Normality-assumption-for-coverage-factor-k-2-(GUM-§6.3.3)","page":"Limitations","text":"The common choice U = 2 · u_c(y) for 95 % coverage assumes the output distribution is approximately normal. When this assumption is violated — small effective degrees of freedom, heavy-tailed input distributions, strongly asymmetric models — a different coverage factor must be used. The welch_satterthwaite helper (forthcoming) and the expanded_uncertainty API (forthcoming) will document this explicitly.","title":"Normality assumption for coverage factor k = 2 (GUM §6.3.3)"},{"category":"section","location":"limitations/#Effective-degrees-of-freedom-ν_eff-30","page":"Limitations","text":"Below ν_eff = 30, the choice k = 2 is inadequate and the Welch-Satterthwaite formula should be used to select an appropriate coverage factor from the Student-t distribution. See the Expanded Uncertainty page for the expanded_uncertainty(m; coverage_probability = …) keyword form that derives k from m.dof via the GUM Table G.2 quantile lookup, and for welch_satterthwaite(contributions, dofs) that computes the effective degrees of freedom itself. A numeric ν_eff < 30 triggers a runtime @warn per REQ-175.","title":"Effective degrees of freedom ν_eff ≥ 30"},{"category":"section","location":"limitations/#Over-warning-on-division-by-zero-and-sqrt-/-log-domain-(M4)","page":"Limitations","text":"The M4 safety warnings (REQ-140, REQ-141) fire whenever the relevant .val field is not a concrete numeric Real. This deliberately errs on the side of over-warning — users who know a symbolic variable is positive (σ > 0) or nonzero still receive the warning, because Symbolics.jl provides no upstream assumptions framework (assume / additionally) through which a library could query \"is this expression provably positive?\". See upstream-bugs.md UB-003 for the ecosystem-level gap and the deferred in-library assume_positive! workaround. Three silencing options are documented on the Display, Substitution, and Safety page.","title":"Over-warning on division-by-zero and sqrt / log domain (M4)"},{"category":"section","location":"limitations/#Independent-software-validation-for-ISO/IEC-17025","page":"Limitations","text":"Use in an ISO/IEC 17025-accreditation calibration laboratory or regulated industry context requires independent software validation per ISO/IEC 17025 §6.4.7. SymbolicUncertainties.jl does not provide such validation and must not be relied upon as the sole uncertainty- evaluation tool in those contexts.","title":"Independent software validation for ISO/IEC 17025"},{"category":"section","location":"limitations/#Copyright-status-of-referenced-standards","page":"Limitations","text":"No normative text from JCGM, IEC, or EA publications is reproduced in the package. References to section numbers in this documentation and in docstrings are provided for traceability only; obtaining the full text of the referenced standards is the user's responsibility.","title":"Copyright status of referenced standards"},{"category":"section","location":"expanded-uncertainty/#Expanded-Uncertainty","page":"Expanded Uncertainty","text":"warning: Coverage factor assumptions\nThe k = 2 shortcut returned by expanded_uncertainty(m) and expanded_uncertainty(m, 2) assumes the output distribution is approximately normal (JCGM 100:2008 §6.3.3) and that the effective degrees of freedom ν_eff ≥ 30. Below that threshold, use the keyword form expanded_uncertainty(m; coverage_probability = …) so that k is derived from m.dof via the Student-t quantile of JCGM 100:2008 §G.4 Table G.2. A numeric ν_eff < 30 also triggers a runtime @warn per REQ-175.\n\nThe expanded uncertainty U = k·u_c(y) is the headline number on every calibration certificate. The GUM §6.2 equation (18) defines it in terms of a coverage factor k and the combined standard uncertainty u_c(y).","title":"Expanded Uncertainty"},{"category":"section","location":"expanded-uncertainty/#Positional-form-—-expanded_uncertainty(m,-k)","page":"Expanded Uncertainty","text":"The positional form multiplies m.err by k, preserving m.val and m.dof. The default k = 2 matches EA-4/02 usage and the GUM Annex H worked examples.\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables Vin σVin R1 σR1 R2 σR2\nVout = propagate(\n    (vin, r1, r2) -> vin * r2 / (r1 + r2),\n    [Vin ± σVin, R1 ± σR1, R2 ± σR2],\n)\n\nU = expanded_uncertainty(Vout)       # k = 2 (default)\nU3 = expanded_uncertainty(Vout, 3)   # k = 3\nU.k, U3.k                            # the field is `U`, not `err`\n\nU.U is k · u_c in closed form. It is not printed here: the voltage divider's combined uncertainty runs to tens of thousands of characters symbolically, which is a fact about the model rather than about the package — see the Uncertainty Budget page for the structured, per-source view of the same quantity.\n\nk may be symbolic; the multiplication is left in closed form.\n\nNegative numeric k raises ArgumentError. Symbolic k is left in closed form without sign inspection.","title":"Positional form — expanded_uncertainty(m, k)"},{"category":"section","location":"expanded-uncertainty/#Keyword-form-—-expanded_uncertainty(m;-coverage_probability)","page":"Expanded Uncertainty","text":"The keyword form derives k from m.dof via an internal lookup into GUM 100:2008 Table G.2 — the Student-t quantile for the given coverage_probability and effective degrees of freedom.\n\nSupported coverage_probability values: {0.68, 0.90, 0.95, 0.99}. Other values raise ArgumentError.\n\n@variables x σx\nm = SymbolicMeasurement(x, σx, Symbolics.Num(4))   # ν_eff = 4\n\nU = expanded_uncertainty(m; coverage_probability = 0.95)\n# k = 2.78 (t-quantile at ν = 4, p = 0.95, GUM Table G.2)\n# Also emits a @warn because ν_eff < 30 (REQ-175)","title":"Keyword form — expanded_uncertainty(m; coverage_probability)"},{"category":"section","location":"expanded-uncertainty/#Dispatch-by-m.dof","page":"Expanded Uncertainty","text":"m.dof value k source Side effects\nnothing (not supplied) Normal quantile @info (diagnostic)\nSymbolics.Num(Inf) Normal quantile none\nSymbolics.Num(n) with n::Real, n ≥ 30 Normal quantile none\nSymbolics.Num(n) with n::Real, 1 ≤ n < 30 Student-t quantile from Table G.2 @warn per REQ-175\nSymbolics.Num(n) with n::Real, n < 1 — ArgumentError\nAny other Num (symbolic) Normal quantile @info (diagnostic)\n\nThe normal-quantile values for the four supported coverage probabilities are:\n\np k (normal)\n0.68 1.00\n0.90 1.645\n0.95 1.96\n0.99 2.576","title":"Dispatch by m.dof"},{"category":"section","location":"expanded-uncertainty/#Welch-Satterthwaite-—-welch_satterthwaite(contributions,-dofs)","page":"Expanded Uncertainty","text":"warning: Coverage factor assumptions\nSee the box at the top of this page — the Welch-Satterthwaite pathway exists precisely to handle the ν_eff < 30 case the k = 2 shortcut cannot.\n\nwelch_satterthwaite(contributions, dofs) returns the symbolic effective degrees of freedom per JCGM 100:2008 §G.4 equation (G.2b):\n\nnu_texteff = fracleft(sum_i u_i^2right)^2\n                           sum_i fracu_i^4nu_i\n\n@variables u1 u2 ν1 ν2\n\nν_eff = welch_satterthwaite([u1, u2], [ν1, ν2])\n# = (u1² + u2²)² / (u1⁴/ν1 + u2⁴/ν2)\n\nMismatched list lengths raise DimensionMismatch. Empty or all-zero inputs return the formal 0/0 symbolic expression unchanged.","title":"Welch-Satterthwaite — welch_satterthwaite(contributions, dofs)"},{"category":"section","location":"expanded-uncertainty/#End-to-end:-from-contributions-to-expanded-uncertainty","page":"Expanded Uncertainty","text":"Combine welch_satterthwaite with the keyword form of expanded_uncertainty to derive the coverage-probability-based expanded uncertainty for a small-sample calibration:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables Vin σVin R1 σR1 R2 σR2\n@variables ν_Vin ν_R1 ν_R2\n\nVout = propagate(\n    (vin, r1, r2) -> vin * r2 / (r1 + r2),\n    [Vin ± σVin, R1 ± σR1, R2 ± σR2],\n)\n\nu_Vin = uncertainty_contribution(Vout, Vin, σVin)\nu_R1  = uncertainty_contribution(Vout, R1,  σR1)\nu_R2  = uncertainty_contribution(Vout, R2,  σR2)\n\n# Evaluated rather than printed: `Symbolics.substitute` inserts the\n# numbers but does not reduce them (see upstream-bugs.md UB-001), so\n# the closed form stays enormous. `build_evaluator` is the tool that\n# actually produces numbers — see the Code Generation page.\nvals = Dict(Vin => 12.0, σVin => 0.1, R1 => 1000.0, σR1 => 10.0,\n            R2 => 2000.0, σR2 => 20.0)\n[\n    Float64(eval(Symbolics.toexpr(Symbolics.substitute(u, vals)))) for\n    u in (u_Vin, u_R1, u_R2)\n]\n\nν_eff = welch_satterthwaite(\n    [u_Vin, u_R1, u_R2],\n    [ν_Vin, ν_R1, ν_R2],\n)\n\nVout_with_dof = SymbolicMeasurement(Vout.val, Vout.err, ν_eff)\nVout_U = expanded_uncertainty(\n    Vout_with_dof;\n    coverage_probability = 0.95,\n)\n\nBecause ν_eff here is a purely symbolic expression (not a concrete Real), the keyword form falls back to the normal quantile and emits an @info message. Supply a concrete numeric substitution via Symbolics.substitute to obtain the t-quantile path and the REQ-175 @warn when appropriate.","title":"End-to-end: from contributions to expanded uncertainty"},{"category":"section","location":"expanded-uncertainty/#API-reference","page":"Expanded Uncertainty","text":"","title":"API reference"},{"category":"section","location":"expanded-uncertainty/#U-is-not-a-standard-uncertainty","page":"Expanded Uncertainty","text":"expanded_uncertainty returns an ExpandedUncertainty, not a SymbolicMeasurement. This is deliberate.\n\nU = k · u_c is the half-width of a coverage interval. It is a reporting quantity, not an input to further propagation: a value carrying U where u_c is expected inflates every downstream result by the coverage factor, silently and by exactly k. Returning a SymbolicMeasurement would have made that substitution type-correct and therefore easy to make by accident.\n\nThe distinct type carries the estimate, U, the coverage factor k that produced it, and the effective degrees of freedom k was derived from — everything a calibration certificate has to state under JCGM 100:2008 §7.2.3, and nothing that invites reuse in a computation.","title":"U is not a standard uncertainty"},{"category":"function","location":"expanded-uncertainty/#SymbolicUncertainties.expanded_uncertainty","page":"Expanded Uncertainty","text":"expanded_uncertainty(m::SymbolicMeasurement, k = 2) -> ExpandedUncertainty\n\nwarning: Coverage factor assumptions\nexpanded_uncertainty(m, k) returns U = k·u_c(y) under the GUM §6.3.3 assumption that the output distribution is approximately normal. When the effective degrees of freedom ν_eff < 30, the shortcut k = 2 for 95 % coverage is inadequate; use the keyword form expanded_uncertainty(m; coverage_probability = …) so that k is derived from m.dof via the Student-t quantile of JCGM 100:2008 §G.4.\n\nReturn a measurement with val = m.val, err = k·m.err, and dof = m.dof (preserved). The default k = 2 matches EA-4/02 usage and the GUM Annex H worked examples.\n\nNegative numeric k raises ArgumentError. Symbolic k is left in closed form with no sign inspection.\n\nImplements the methodology of JCGM 100:2008 §6.2 equation (18). Traces REQ-050.\n\n\n\n\n\nexpanded_uncertainty(m; coverage_probability=nothing) -> ExpandedUncertainty\n\nSingle-argument convenience wrapper. With no keyword argument it applies the default k = 2, matching EA-4/02 usage. With an explicit coverage_probability, it derives k from m.dof via the Student-t quantile of JCGM 100:2008 §G.4 Table G.2.\n\nwarning: Coverage factor assumptions\nThe keyword form derives k from m.dof via the Student-t quantile of JCGM 100:2008 §G.4 Table G.2. When m.dof is nothing or a symbolic expression, the normal-distribution quantile is used instead (per GUM §6.3.3); a numeric ν_eff < 30 emits a runtime @warn.\n\nSupported coverage_probability values: {0.68, 0.90, 0.95, 0.99} — the four entries of GUM Table G.2. Other values raise ArgumentError.\n\nSee expanded_uncertainty(m, k) for the positional form.\n\nImplements the methodology of JCGM 100:2008 §6.2 / §G.4. Traces REQ-050, REQ-051, REQ-174, REQ-175.\n\n\n\n\n\n","title":"SymbolicUncertainties.expanded_uncertainty"},{"category":"type","location":"expanded-uncertainty/#SymbolicUncertainties.ExpandedUncertainty","page":"Expanded Uncertainty","text":"ExpandedUncertainty\n\nResult of an expanded-uncertainty calculation: the estimate val, the expanded uncertainty U = k · u_c, the coverage factor k, and the effective degrees of freedom the factor was derived from.\n\nDeliberately not a SymbolicMeasurement. U is a coverage interval half-width, not a standard uncertainty, so a quantity carrying it is not propagatable: feeding U back into a computation would silently inflate every downstream result by k. Returning a distinct type makes that mistake impossible to make by accident (JCGM 100:2008 §6.2).\n\nTraces REQ-050, REQ-051.\n\n\n\n\n\n","title":"SymbolicUncertainties.ExpandedUncertainty"},{"category":"function","location":"expanded-uncertainty/#SymbolicUncertainties.welch_satterthwaite","page":"Expanded Uncertainty","text":"welch_satterthwaite(contributions, dofs) -> Num\n\nwarning: Coverage factor assumptions\nThe Welch-Satterthwaite effective degrees of freedom support the Student-t pathway of expanded_uncertainty(m; coverage_probability). The k = 2 shortcut of expanded_uncertainty(m, 2) assumes ν_eff ≥ 30; below that threshold, JCGM 100:2008 §6.3.3 requires the t-quantile path this function enables.\n\nReturn the symbolic effective degrees of freedom ν_eff per JCGM 100:2008 §G.4 equation (G.2b):\n\nν_eff = (Σᵢ uᵢ²)² / Σᵢ (uᵢ⁴ / νᵢ)\n\nwhere uᵢ are the per-variable uncertainty contributions (typically from uncertainty_contribution) and νᵢ the associated degrees of freedom.\n\nRaises DimensionMismatch when the two lists differ in length.\n\nEdge case: empty lists or all-zero contributions produce the formal 0/0 symbolic expression unchanged — no special-case substitution to Inf.\n\nImplements the methodology of JCGM 100:2008 §G.4. Traces REQ-052, REQ-174.\n\n\n\n\n\n","title":"SymbolicUncertainties.welch_satterthwaite"},{"category":"section","location":"getting-started/#Getting-Started","page":"Getting Started","text":"This page walks you through the core SymbolicUncertainties.jl workflow in under 5 minutes: declare symbolic measurements, combine them with ordinary arithmetic, and read off the closed-form uncertainty expressions that JCGM 100:2008 (GUM) prescribes.\n\nwarning: Regulated-use disclaimer\nSymbolicUncertainties.jl implements the methodology of JCGM 100:2008 but has not been independently validated for calibration- accreditation contexts. See the Limitations page and LICENSE.md for the full non-warranty clause.\n\nnote: Notation: `±` and the combined standard uncertainty\nIn this package, a ± b builds a measurement whose uncertainty field is a combined standard uncertainty u_c, matching the Julia ecosystem convention established by Measurements.jl. This departs from JCGM 100:2008 §7.2.2, which recommends presenting a result with its combined standard uncertainty using one of four textual forms (for example m_S = 100.02147 g with u_c = 0.35 mg or the parenthetical form m_S = 100.02147(35) g) and deliberately avoids the ± glyph because it is historically associated with the expanded uncertainty y ± U = y ± k·u_c defined in §6.2. When producing a GUM-conformant calibration report, do not copy the Unicode ± display verbatim — use one of the four §7.2.2 textual forms instead.","title":"Getting Started"},{"category":"section","location":"getting-started/#Declare-symbolic-variables","page":"Getting Started","text":"using Symbolics\nusing SymbolicUncertainties\n\n@variables V σV I σI\nnothing # hide\n\nBoth Symbolics (for @variables) and SymbolicUncertainties (for SymbolicMeasurement and ±) need to be loaded.","title":"Declare symbolic variables"},{"category":"section","location":"getting-started/#Build-a-measurement-with","page":"Getting Started","text":"The infix constructor ± pairs an estimate with its standard uncertainty:\n\nV_m = V ± σV\nI_m = I ± σI\n\nEither operand can be a Symbolics.Num, a plain Number, or a mixture of the two. Plain numbers are promoted to symbolic literals automatically:\n\nSymbolicMeasurement(1.5, 0.1)     # numeric constructor\n\nA negative numeric standard uncertainty is refused at construction time — the standard uncertainty u_c must be non-negative per JCGM 100:2008 §4.3.1:\n\njulia> SymbolicMeasurement(1.5, -0.1)\nERROR: ArgumentError: standard uncertainty must be non-negative (GUM §4.3.1)","title":"Build a measurement with ±"},{"category":"section","location":"getting-started/#Combine-measurements-with-the-five-binary-operators","page":"Getting Started","text":"All five standard arithmetic operators (+, -, *, /, ^) are defined on SymbolicMeasurement. Each returns a new SymbolicMeasurement whose err field carries the propagated uncertainty expression implementing the relevant GUM §5.1 formula.\n\nV_m + I_m     # sum            — REQ-010\nV_m - I_m     # difference     — REQ-011\nV_m * I_m     # product        — REQ-012\nV_m^2         # integer power  — REQ-014\nV_m / I_m     # quotient       — REQ-013\n\nMixed-mode expressions with plain numbers or bare symbolic variables also work — the non-measurement operand is treated as if it had zero uncertainty:\n\n2 * V_m       # equivalent to (2 ± 0) * V_m\nV_m / I       # I is a bare Symbolics.Num, not a measurement","title":"Combine measurements with the five binary operators"},{"category":"section","location":"getting-started/#Worked-example-—-Ohm's-law","page":"Getting Started","text":"The textbook GUM example R = V / I illustrates the quotient rule for uncertainty propagation.\n\nR_m = V_m / I_m\n\nThe resulting measurement's err field is algebraically equivalent to the GUM §5.1 reference form\n\nu_R = fracVIsqrtleft(fracsigma_VVright)^2 +\n                          left(fracsigma_IIright)^2\n\nwhich you can reconstruct directly with Symbolics:\n\nreference_err = (V / I) * sqrt((σV / V)^2 + (σI / I)^2)\n\nNumerical substitution confirms the equivalence:\n\ndict = Dict(V => 12.0, σV => 0.1, I => 0.5, σI => 0.005)\n(\n    Symbolics.substitute(R_m.err, dict),\n    Symbolics.substitute(reference_err, dict),\n)\n\nnote: Repeated operands are exact\nThe binary operators track operand identity. If the same measurement appears several times in an expression, the shared source is recognised and its sensitivities combine before the variance is formed:julia> x = 8.4 ± 0.7\n8.4 ± 0.7\n\njulia> x - x\n0.0 ± 0\n\njulia> x / x\n1 ± 0\n\njulia> x + x\n16.8 ± sqrt(1.9599999999999997)(x + x is 2x with uncertainty 2σ, not σ√2.)Before milestone M11 this was not so: each occurrence counted as an independent input, x - x reported σ√2, and propagate was the documented workaround. That distinction is gone — a quantity now records which independent sources it derives from, so there is no longer a right and a wrong way to write the same model.Two measurements built by two separate ± calls remain independent, even when written with the same symbols: two resistors of equal nominal tolerance are not the same resistor.See Uncertainty Sources for the representation this rests on.","title":"Worked example — Ohm's law"},{"category":"section","location":"getting-started/#Printing","page":"Getting Started","text":"A SymbolicMeasurement prints as val ± err in Unicode, with an ASCII +/- fallback in contexts where Unicode is unavailable (for example, plain-text log pipelines):\n\nshow(stdout, V_m)\nprintln()\nshow(IOContext(stdout, :unicode => false), V_m)","title":"Printing"},{"category":"section","location":"getting-started/#What's-next","page":"Getting Started","text":"The next milestone (M2) will add mathematical functions — sin, cos, log, exp, sqrt, and friends — via automatic symbolic differentiation, plus multi-variable propagation through user-supplied functions (propagate(f, measurements)). These let you handle the classical voltage-divider and RC examples and the full uncertainty-budget workflow (EA-4/02 §7.3 presentation built on top of JCGM 100:2008 §5.1.3 sensitivity coefficients).","title":"What's next"},{"category":"section","location":"getting-started/#API-reference-—-Milestone-M1","page":"Getting Started","text":"","title":"API reference — Milestone M1"},{"category":"section","location":"getting-started/#The-core-type-and-its-constructors","page":"Getting Started","text":"","title":"The core type and its constructors"},{"category":"section","location":"getting-started/#Binary-arithmetic-operators","page":"Getting Started","text":"","title":"Binary arithmetic operators"},{"category":"section","location":"getting-started/#Display","page":"Getting Started","text":"","title":"Display"},{"category":"type","location":"getting-started/#SymbolicUncertainties.SymbolicMeasurement","page":"Getting Started","text":"SymbolicMeasurement\n\nA measurand recorded in the canonical JCGM 100:2008 estimate ± standard uncertainty form, using Symbolics.jl expressions throughout.\n\nImplements the methodology of JCGM 100:2008 §4.1 (estimate of the measurand), §5.1 (combined standard uncertainty), and §G.4 (effective degrees of freedom).\n\nSince M11 the quantity records which independent sources it derives from and with what sensitivity, rather than a bare uncertainty number. Every combined uncertainty, sensitivity coefficient, budget and covariance is derived from that structure.\n\nFields:\n\nval::Symbolics.Num — symbolic expression for the estimate of the measurand (JCGM 100:2008 §4.1).\nterms::Dict{SourceId,Symbolics.Num} — ∂val/∂source for each independent source the quantity depends on (JCGM 100:2008 §5.1.3, sensitivity coefficients cᵢ).\nsources::Dict{SourceId,Source} — descriptor of each source: its standard uncertainty, optional degrees of freedom, display name. Carried by the quantity rather than held in module state, so that Symbolics.substitute stays local (REQ-120, REQ-123).\ncov::Dict{Tuple{SourceId,SourceId},Symbolics.Num} — declared covariances between sources (JCGM 100:2008 §5.2.2). Empty by default: sources are independent unless declared otherwise.\n\nProperties:\n\nm.err — combined standard uncertainty u_c, computed from the fields above (JCGM 100:2008 §5.1), no longer a stored field.\nm.dof — effective degrees of freedom ν_eff (§G.4).\n\nSymbolicMeasurement is deliberately not a subtype of Real or AbstractFloat — this avoids implicit promotion loops with the Julia numeric tower and protects the symbolic uncertainty information from being silently dropped during generic numeric code.\n\nTraces REQ-001, REQ-002, REQ-006, REQ-200.\n\n\n\n\n\n","title":"SymbolicUncertainties.SymbolicMeasurement"},{"category":"method","location":"getting-started/#SymbolicUncertainties.SymbolicMeasurement-Tuple{Symbolics.Num, Symbolics.Num}","page":"Getting Started","text":"SymbolicMeasurement(val::Symbolics.Num, err::Symbolics.Num)\n\nBuild a SymbolicMeasurement from two Symbolics.Num operands, leaving the effective degrees of freedom unset (dof = nothing).\n\nImplements the methodology of JCGM 100:2008 §4.1 + §5.1. Traces REQ-002, REQ-003.\n\n\n\n\n\n","title":"SymbolicUncertainties.SymbolicMeasurement"},{"category":"method","location":"getting-started/#SymbolicUncertainties.SymbolicMeasurement-Tuple{Number, Number}","page":"Getting Started","text":"SymbolicMeasurement(val::Number, err::Number)\n\nBuild a SymbolicMeasurement from two plain numeric values, promoting both to Symbolics.Num literals so that the full symbolic pipeline applies uniformly to downstream operations.\n\nRaises ArgumentError if err is a negative Real — the standard uncertainty must be non-negative per JCGM 100:2008 §4.3.1.\n\nImplements the methodology of JCGM 100:2008 §4.3.1. Traces REQ-004, REQ-005.\n\n\n\n\n\n","title":"SymbolicUncertainties.SymbolicMeasurement"},{"category":"method","location":"getting-started/#SymbolicUncertainties.:±-Tuple{Symbolics.Num, Symbolics.Num}","page":"Getting Started","text":"±(a, u)\n\nInfix constructor for SymbolicMeasurement. The left operand is the estimate of the measurand; the right operand is its standard uncertainty. Either operand may be a Symbolics.Num or a plain Number; numeric operands are promoted to Num literals.\n\n± denotes the standard uncertainty (u_c), not the expanded uncertainty (U = k · u_c), consistent with JCGM 100:2008 §6.\n\nImplements the methodology of JCGM 100:2008 §6. Traces REQ-003.\n\n\n\n\n\n","title":"SymbolicUncertainties.:±"},{"category":"method","location":"getting-started/#Base.:+-Tuple{SymbolicMeasurement, SymbolicMeasurement}","page":"Getting Started","text":"Base.:+(x::SymbolicMeasurement, y::SymbolicMeasurement)\n\nPropagate a sum per JCGM 100:2008 §5.1.3: val = x.val + y.val, with sensitivity coefficients cₓ = cᵧ = 1.\n\nImplements the methodology of JCGM 100:2008 §5.1. Traces REQ-010, REQ-110, REQ-202.\n\n\n\n\n\n","title":"Base.:+"},{"category":"method","location":"getting-started/#Base.:--Tuple{SymbolicMeasurement, SymbolicMeasurement}","page":"Getting Started","text":"Base.:-(x::SymbolicMeasurement, y::SymbolicMeasurement)\n\nPropagate a difference per JCGM 100:2008 §5.1.3: val = x.val - y.val, with sensitivity coefficients cₓ = 1, cᵧ = -1.\n\nThe signs are retained rather than squared away, which is what makes x - x exactly zero when both operands are the same measurement.\n\nImplements the methodology of JCGM 100:2008 §5.1. Traces REQ-011, REQ-110, REQ-202.\n\n\n\n\n\n","title":"Base.:-"},{"category":"method","location":"getting-started/#Base.:*-Tuple{SymbolicMeasurement, SymbolicMeasurement}","page":"Getting Started","text":"Base.:*(x::SymbolicMeasurement, y::SymbolicMeasurement)\n\nPropagate a product per JCGM 100:2008 §5.1.3: val = x.val · y.val, with sensitivity coefficients cₓ = y.val, cᵧ = x.val.\n\nImplements the methodology of JCGM 100:2008 §5.1. Traces REQ-012, REQ-110.\n\n\n\n\n\n","title":"Base.:*"},{"category":"method","location":"getting-started/#Base.:/-Tuple{SymbolicMeasurement, SymbolicMeasurement}","page":"Getting Started","text":"Base.:/(x::SymbolicMeasurement, y::SymbolicMeasurement)\n\nPropagate a quotient per JCGM 100:2008 §5.1.3: val = x.val / y.val, with sensitivity coefficients cₓ = 1/y.val, cᵧ = -x.val/y.val².\n\nThis is the operator behind the Ohm's-law worked example (R = V/I), the M1 exit gate. Since M11 it also makes x / x exactly dimensionless-one with zero uncertainty.\n\nImplements the methodology of JCGM 100:2008 §5.1. Traces REQ-013, REQ-110, REQ-202.\n\n\n\n\n\n","title":"Base.:/"},{"category":"method","location":"getting-started/#Base.:^-Tuple{SymbolicMeasurement, Real}","page":"Getting Started","text":"Base.:^(x::SymbolicMeasurement, n::Union{Real, Symbolics.Num})\n\nPropagate a power per JCGM 100:2008 §5.1.3: val = x.val^n, with sensitivity coefficient cₓ = n · x.val^(n-1).\n\nThe exponent is a plain Real or a Symbolics.Num, never another measurement.\n\nImplements the methodology of JCGM 100:2008 §5.1. Traces REQ-014, REQ-110.\n\n\n\n\n\n","title":"Base.:^"},{"category":"method","location":"getting-started/#Base.show-Tuple{IO, SymbolicMeasurement}","page":"Getting Started","text":"Base.show(io::IO, m::SymbolicMeasurement)\n\nDisplay a SymbolicMeasurement as val ± err in Unicode when the output context supports it, falling back to val +/- err in plain ASCII when the context carries :unicode => false.\n\nTraces REQ-111.\n\n\n\n\n\n","title":"Base.show"},{"category":"section","location":"dimensional-analysis/#Dimensional-Analysis","page":"Dimensional Analysis","text":"Load DynamicQuantities.jl and check_units will tell you whether a model holds together dimensionally.\n\nIt returns a report and never throws. A unit mistake is something you want to be told about, not something that should stop you mid-derivation — which is what the pre-M12 construction-time DimensionError did.\n\nusing Symbolics, SymbolicUncertainties, DynamicQuantities\n\n@variables V I\ncheck_units(V / I, Dict(V => u\"V\", I => u\"A\"))\n# UnitReport: consistent\n\ncheck_units(V + I, Dict(V => u\"V\", I => u\"A\"))\n# UnitReport: 1 finding(s)\n#   • + in `I + V` — dimensions differ (A vs m² kg s⁻³ A⁻¹)\n\nUnits are an annotation of the symbol, supplied in a dictionary, never a value inside the expression. ModelingToolkit makes the same choice — it holds units in variable metadata — and the reason is practical: a quantity living inside the expression tree would be dragged through every simplification and every derivative.","title":"Dimensional Analysis"},{"category":"section","location":"dimensional-analysis/#Checking-a-measurement","page":"Dimensional Analysis","text":"Passing a SymbolicMeasurement also checks something no other tool does: that the combined uncertainty carries the dimension of the measurand. It is the most common unit error in a budget, and it is structurally invisible elsewhere because y and u live in different fields.\n\n@variables σV σI\nm = (V ± σV) / (I ± σI)\ncheck_units(m, Dict(V => u\"V\", σV => u\"V\", I => u\"A\", σI => u\"A\"))","title":"Checking a measurement"},{"category":"section","location":"dimensional-analysis/#Dimension-is-not-enough","page":"Dimensional Analysis","text":"Three cases defeat a plain dimension check. Each has its own annotation, because dimensional equality is necessary but never sufficient to decide that two quantities may be combined.","title":"Dimension is not enough"},{"category":"section","location":"dimensional-analysis/#Affine-scales-—-C,-F","page":"Dimensional Analysis","text":"20 °C + 20 °C is not 40 °C. The zero of an affine scale is conventional, so absolute values do not add; only differences are proper intervals on the base unit. Both sides carry dimension Θ, so a dimension check passes it without a murmur.\n\nusing SymbolicUncertainties: Affine, Scaled, Kind\n@variables T1 T2\ncelsius = Affine(u\"K\", :celsius, 273.15)\n\ncheck_units(T1 + T2, Dict(T1 => celsius, T2 => celsius))  # reports\ncheck_units(T1 - T2, Dict(T1 => celsius, T2 => celsius))  # consistent\n\nThe metrological corollary matters more than the arithmetic one: an uncertainty stated in °C is a kelvin interval, never an absolute temperature. A dispersion has no origin (JCGM 100:2008 §4.3.1).","title":"Affine scales — °C, °F"},{"category":"section","location":"dimensional-analysis/#Dimensionless-is-not-one-thing-—-%,-ppm,-dB","page":"Dimensional Analysis","text":"All three are dimensionless and none is interchangeable with another. dB is additionally logarithmic, so it does not even combine additively the way % and ppm do.\n\npct = Scaled(:percent, 1e-2)\nppm = Scaled(:ppm, 1e-6)\ndB  = Scaled(:dB, nothing; logarithmic = true)","title":"Dimensionless is not one thing — %, ppm, dB"},{"category":"section","location":"dimensional-analysis/#Same-dimension,-different-quantity","page":"Dimensional Analysis","text":"Torque and energy are both N·m. Activity and frequency are both s⁻¹. Adding a torque to an energy is a modelling error that dimensional analysis alone declares perfectly valid.\n\n@variables M E\ntorque = Kind(u\"N*m\", :torque)\nenergy = Kind(u\"N*m\", :energy)\n\ncheck_units(M + E, Dict(M => torque, E => energy))   # reports\n\nPer VIM §1.1 a quantity is not defined by its dimension; Kind records the part the dimension leaves out.","title":"Same dimension, different quantity"},{"category":"section","location":"dimensional-analysis/#Why-DynamicQuantities-rather-than-Unitful","page":"Dimensional Analysis","text":"Unitful encodes units in the type parameter, so every distinct unit combination is a distinct concrete type. DynamicQuantities keeps dimensions in a runtime field, so one type covers V, A, Ω and W alike. That matters here because an uncertainty budget's rows are deliberately heterogeneous in unit.\n\nIt is also the system ModelingToolkit actually validates: its screen_unit accepts DynamicQuantities quantities and passes Unitful metadata through unchecked. Since the package ships an MTK extension, matching that avoids a backend switch mid-model.\n\nBoth packages export @u_str, so using them together makes u\"V\" ambiguous. That is not a reason to pick one — DynamicQuantities' own README shows the coexistence idiom, using DynamicQuantities; import Unitful, and a qualified Unitful.u\"V\" works fine. It is simply a thing to know if you keep both loaded.","title":"Why DynamicQuantities rather than Unitful"},{"category":"section","location":"dimensional-analysis/#API","page":"Dimensional Analysis","text":"","title":"API"},{"category":"function","location":"dimensional-analysis/#SymbolicUncertainties.check_units","page":"Dimensional Analysis","text":"check_units(expr, units::AbstractDict) -> UnitReport\n\nCheck an expression, or a SymbolicMeasurement, for dimensional consistency under the supplied symbol annotations.\n\nReturns a report; never throws on an inconsistency. Annotations may be DynamicQuantities quantities or one of Affine, Scaled, Kind for the cases a bare dimension cannot express.\n\nRequires DynamicQuantities.jl to be loaded.\n\nImplements the methodology of JCGM 100:2008 §4.1 read with VIM §1.1. Traces REQ-100, REQ-101, REQ-102.\n\n\n\n\n\n","title":"SymbolicUncertainties.check_units"},{"category":"type","location":"dimensional-analysis/#SymbolicUncertainties.UnitReport","page":"Dimensional Analysis","text":"UnitReport\n\nResult of check_units: the findings, in the order they were encountered. An empty report means the expression is dimensionally sound under the supplied annotations.\n\nA report is data, not an exception — inspect it, print it, or ignore it. Nothing in the package changes behaviour based on its contents.\n\n\n\n\n\n","title":"SymbolicUncertainties.UnitReport"},{"category":"type","location":"dimensional-analysis/#SymbolicUncertainties.UnitFinding","page":"Dimensional Analysis","text":"UnitFinding\n\nOne dimensional inconsistency: what names the offending subexpression, why explains the rule it breaks, and details carries the conflicting annotations.\n\n\n\n\n\n","title":"SymbolicUncertainties.UnitFinding"},{"category":"function","location":"dimensional-analysis/#SymbolicUncertainties.is_consistent","page":"Dimensional Analysis","text":"is_consistent(r::UnitReport) -> Bool\n\ntrue when the report holds no findings.\n\n\n\n\n\n","title":"SymbolicUncertainties.is_consistent"},{"category":"type","location":"dimensional-analysis/#SymbolicUncertainties.Affine","page":"Dimensional Analysis","text":"Affine(base, name, offset)\n\nAn affine-scale quantity such as °C or °F: its zero is conventional, so absolute values do not add, and only differences are proper intervals on base.\n\n20 °C + 20 °C is not 40 °C, yet both sides carry dimension Θ and a dimension check passes it. An uncertainty stated in °C is a kelvin interval, never an absolute temperature (JCGM 100:2008 §4.3.1: u is a dispersion, and a dispersion has no origin).\n\nTraces REQ-101.\n\n\n\n\n\n","title":"SymbolicUncertainties.Affine"},{"category":"type","location":"dimensional-analysis/#SymbolicUncertainties.Scaled","page":"Dimensional Analysis","text":"Scaled(name, factor; logarithmic = false)\n\nA dimensionless ratio carrying a scale: %, ppm, dB.\n\nAll are dimensionless and none is interchangeable with another, which dimensional equality cannot see. dB is additionally logarithmic, so it does not even combine additively the way % and ppm do.\n\nTraces REQ-102.\n\n\n\n\n\n","title":"SymbolicUncertainties.Scaled"},{"category":"type","location":"dimensional-analysis/#SymbolicUncertainties.Kind","page":"Dimensional Analysis","text":"Kind(unit, kind)\n\nA quantity distinguished by more than its dimension.\n\nTorque and energy are both N·m; activity and frequency are both s⁻¹. Adding a torque to an energy is a modelling error that dimensional analysis alone declares valid. Per VIM §1.1 a quantity is not defined by its dimension, and Kind records the part the dimension omits.\n\nTraces REQ-100.\n\n\n\n\n\n","title":"SymbolicUncertainties.Kind"},{"category":"function","location":"dimensional-analysis/#SymbolicUncertainties.interval_unit","page":"Dimensional Analysis","text":"interval_unit(a::Affine)\n\nThe unit a difference of two a values carries — the one an uncertainty in a is actually expressed in.\n\n\n\n\n\n","title":"SymbolicUncertainties.interval_unit"},{"category":"section","location":"power-resistance-metrology/#Power-and-Resistance-Measurement:-a-Metrology-Walkthrough","page":"Power & Resistance Metrology","text":"This tutorial is a full metrological worked example of measuring the electrical power dissipated in a precision resistor and assessing conformity of the resistor against its nominal value. It ties GUM methodology (JCGM 100:2008) to VIM vocabulary (JCGM 200:2012) and shows how the SymbolicUncertainties.jl API fits the calibration-certificate workflow end-to-end.\n\nUnlike the single-section summaries in Worked Examples, this page walks through a complete uncertainty budget, coverage-interval construction, conformity assessment, and reporting — the sequence a calibration lab would follow in practice.","title":"Power & Resistance Measurement: a Metrology Walkthrough"},{"category":"section","location":"power-resistance-metrology/#VIM-terms-used-in-this-tutorial","page":"Power & Resistance Metrology","text":"VIM § Term Role here\n2.3 Measurand The quantities we want — R (resistance of the device under test) and P (power dissipated).\n2.10 Measured quantity value The numerical result of the measurement — R_meas, P_meas.\n2.11 Nominal quantity value The rated / stamped value R₀ = 100 Ω (E96 series).\n2.16 Measurement error R_meas − R₀ (unknown exactly; bounded by uncertainty).\n2.26 Measurement uncertainty u(y) Standard uncertainty on the result.\n2.35 Coverage probability Here p = 0.95, i.e. 95 % confidence.\n2.36 Coverage interval The interval y ± U, where U = k · u(y).\n2.37 Coverage factor k = 2 for p ≈ 0.95 under a normal distribution (GUM §6.3.3).\n4.17 Conformity assessment Decision: is the device within its specification limits?\n4.18 Calibration certificate The reporting artifact — see final section.\n\nFor full VIM definitions see JCGM 200:2012; this tutorial paraphrases only.","title":"VIM terms used in this tutorial"},{"category":"section","location":"power-resistance-metrology/#Scenario","page":"Power & Resistance Metrology","text":"A calibration lab receives a precision resistor rated at R₀ = 100 Ω (an E96 series value) with a manufacturer tolerance of ±0.1 %. The lab applies a stable voltage source and measures both voltage across and current through the resistor using calibrated instruments:\n\nVoltmeter across the resistor — reading V = 10.000 V with σV = 0.001 V (1 mV standard uncertainty).\nAmmeter in series — reading I = 0.100 02 A with σI = 1.0 × 10⁻⁵ A (10 μA standard uncertainty).\n\nThe lab needs to deliver:\n\nP — power dissipated, with uncertainty u(P).\nR_meas — resistance inferred from Ohm's law, with uncertainty u(R).\nA conformity assessment — is R_meas consistent with R₀ = 100 Ω at 95 % coverage?\n\nInputs are treated as independent (no correlated drift between the voltmeter and ammeter readings) — a standard JCGM 100:2008 §5.1.2 eq (10) setup.","title":"Scenario"},{"category":"section","location":"power-resistance-metrology/#Build-the-measurement-models","page":"Power & Resistance Metrology","text":"using SymbolicUncertainties\nusing SymbolicUncertainties: ±\nusing Symbolics\n\n# Symbolic inputs — values and standard uncertainties\n@variables V I σV σI\n\n# Model 1: power P = V · I\nP = propagate((v, i) -> v * i, [V ± σV, I ± σI])\n# P.val = V * I\n# P.err = sqrt((I·σV)² + (V·σI)²)   — uncorrelated-inputs form\n\n# Model 2: resistance R = V / I\nR = propagate((v, i) -> v / i, [V ± σV, I ± σI])\n# R.val = V / I\n# R.err = sqrt((σV/I)² + (V·σI/I²)²)\n\npropagate is used (rather than the binary * / / operators) because the underlying sensitivity analysis is explicit and the result is guaranteed to collapse repeated symbolic-variable occurrences — see Sensitivity Analysis.","title":"Build the measurement models"},{"category":"section","location":"power-resistance-metrology/#Power-budget","page":"Power & Resistance Metrology","text":"P_rows = uncertainty_budget(P, [V, I], [σV, σI])\n# Vector{NamedTuple} with 2 rows — one per input.\n# Columns: variable, sigma, sensitivity, contribution,\n#          relative (fraction of variance).\n\nEach row follows the GUM §5.1.6 / EA-4/02 §7.3 schema. Substituting numerics:\n\ndict = Dict(V => 10.000, I => 0.10002, σV => 1e-3, σI => 1e-5)\n\nP_num = Symbolics.substitute(P, dict)\n# P_num.val ≈ 1.0002  W\n# P_num.err ≈ sqrt((0.10002·1e-3)² + (10·1e-5)²)\n#           ≈ sqrt(1.00e-8 + 1.00e-8)\n#           ≈ 1.414e-4  W\n\nDominant source:\n\ndominant = dominant_source(P, [V, I], [σV, σI];\n                           values = dict)\n# → returns `V` or `I`; in this setup they contribute\n#   nearly equally (1.00e-8 each), so the dominant source\n#   depends on tie-breaking.\n\nThis is a balanced budget — a deliberate choice in the scenario design. Real calibration work usually reveals one dominant source on which to focus improvements.","title":"Power budget"},{"category":"section","location":"power-resistance-metrology/#Resistance-budget","page":"Power & Resistance Metrology","text":"R_rows = uncertainty_budget(R, [V, I], [σV, σI])\n\nR_num = Symbolics.substitute(R, dict)\n# R_num.val = 10.000 / 0.10002 ≈ 99.98 Ω\n# R_num.err ≈ sqrt((1e-3/0.10002)² +\n#                  (10·1e-5/0.10002²)²)\n#           ≈ sqrt(1.00e-4 + 1.00e-4)\n#           ≈ 1.414e-2 Ω   ≈ 14 mΩ","title":"Resistance budget"},{"category":"section","location":"power-resistance-metrology/#Coverage-interval-(VIM-§2.36)","page":"Power & Resistance Metrology","text":"Expand u(R) to a coverage interval with k = 2 (p ≈ 0.95, assuming approximate normality per GUM §6.3.3):\n\nR_expanded = expanded_uncertainty(R, 2)\n# R_expanded.val = R.val        (unchanged)\n# R_expanded.U = 2 · R.err\n\nR_exp_num = Symbolics.substitute(R_expanded, dict)\n# R_exp_num.val ≈ 99.98  Ω\n# R_exp_num.U ≈ 2 · 14 mΩ ≈ 28 mΩ\n\nThe coverage interval is therefore [99.98 − 0.028, 99.98 + 0.028] Ω = [99.952, 100.008] Ω.","title":"Coverage interval (VIM §2.36)"},{"category":"section","location":"power-resistance-metrology/#Conformity-assessment-(VIM-§4.17)","page":"Power & Resistance Metrology","text":"Question: is R_meas = 99.98 Ω consistent with R₀ = 100 Ω at 95 % coverage?\n\nR₀ = 100.0\nlower = R_exp_num.val - R_exp_num.U\nupper = R_exp_num.val + R_exp_num.U\npass = lower ≤ R₀ ≤ upper\n# → true : R₀ = 100 ∈ [99.952, 100.008]  ✓\n\nResult: R₀ lies inside the 95 % coverage interval, so the resistor is consistent with its nominal value at the 95 % level. The calibration certificate reports R = 99.98 Ω ± 0.028 Ω (k = 2, p ≈ 0.95) and the conformity status PASS.","title":"Conformity assessment (VIM §4.17)"},{"category":"section","location":"power-resistance-metrology/#Counter-example-—-a-failing-conformity-check","page":"Power & Resistance Metrology","text":"Suppose instead the ammeter reading had been I = 0.10050 A (a 0.48 % drift):\n\ndict_fail = Dict(V => 10.000, I => 0.10050,\n                 σV => 1e-3, σI => 1e-5)\n\nR_fail = Symbolics.substitute(R_expanded, dict_fail)\n# R_fail.val ≈ 99.502 Ω\n# R_fail.U ≈ 28 mΩ   (roughly unchanged)\n# coverage interval: [99.474, 99.530]\n\nlower_f = R_fail.val - R_fail.U\nupper_f = R_fail.val + R_fail.U\npass_f = lower_f ≤ R₀ ≤ upper_f\n# → false : R₀ = 100 ∉ [99.474, 99.530]  ✗\n\nResult: the measured resistance is inconsistent with the nominal value at 95 % coverage. The certificate reports NON-CONFORM and the device is tagged for adjustment or rejection.","title":"Counter-example — a failing conformity check"},{"category":"section","location":"power-resistance-metrology/#Reporting-(GUM-§7-VIM-§4.18)","page":"Power & Resistance Metrology","text":"A calibration-certificate-ready summary block:\n\nfunction report(label, m, k)\n    v = Symbolics.value(Symbolics.substitute(m.val, Dict()))\n    u = Symbolics.value(Symbolics.substitute(m.err, Dict()))\n    # (in practice m is already fully substituted — the\n    # `substitute(_, Dict())` round-trip is just defensive)\n    println(label, \" = \", round(v, sigdigits = 5),\n            \" ± \", round(k*u, sigdigits = 3),\n            \"  (k = \", k, \", coverage ≈ \",\n            k == 2 ? \"95 %\" : \"…\", \")\")\nend\n\nreport(\"P\", P_num, 2)\n# P = 1.0002 ± 2.83e-4  (k = 2, coverage ≈ 95 %)\n\nreport(\"R\", R_num, 2)\n# R = 99.98 ± 2.83e-2   (k = 2, coverage ≈ 95 %)\n\nCertificate excerpt (human-readable):\n\nDevice under test: precision resistor, nominal R₀ = 100.000 Ω (E96 series).Conditions: T = 23.0 °C, applied voltage V_applied ≈ 10 V.Result:R = 99.98 Ω ± 0.028 Ω (k = 2, coverage ≈ 95 %).\nP = 1.0002 W ± 0.00028 W (k = 2, coverage ≈ 95 %).Dominant uncertainty sources: voltmeter reading and ammeter reading contribute approximately equally (~50 % each).Conformity: PASS — R₀ = 100 Ω lies within the 95 % coverage interval [99.952, 100.008] Ω.Method: JCGM 100:2008 §5.1.2 eq (10), uncorrelated inputs; software implementation: SymbolicUncertainties.jl v0.10.0.","title":"Reporting (GUM §7 + VIM §4.18)"},{"category":"section","location":"power-resistance-metrology/#What-this-tutorial-demonstrates","page":"Power & Resistance Metrology","text":"Symbolic propagation is calibration-ready. Every intermediate expression in this walkthrough is symbolic until the final Symbolics.substitute, giving the lab a traceable derivation of the final number.\nVIM vocabulary maps cleanly to the API: measurand → SymbolicMeasurement.val, measurement uncertainty → .err, coverage interval → expanded_uncertainty(m, k), dominant source → dominant_source(...).\nConformity assessment is a downstream consumer of the coverage interval — the library provides the interval; the decision logic (R₀ ∈ [y-U, y+U]) is two lines of user code.\nParaphrase-only — no normative JCGM / VIM text is reproduced in the library or this tutorial. Section numbers are cited to anchor the methodology; the authoritative document is JCGM 100:2008 / JCGM 200:2012 itself.","title":"What this tutorial demonstrates"},{"category":"section","location":"power-resistance-metrology/#References","page":"Power & Resistance Metrology","text":"JCGM 100:2008 — Evaluation of measurement data — Guide to the expression of uncertainty in measurement (GUM). BIPM. See CITATION.bib at the repository root for the BibTeX entry.\nJCGM 200:2012 — International vocabulary of metrology — Basic and general concepts and associated terms (VIM), 3rd edition. BIPM.\nEA-4/02 M:2022 — Evaluation of the Uncertainty of Measurement in Calibration. European co-operation for Accreditation.\nFor the full methodology reference of every SymbolicUncertainties.jl export, see Methodology Reference.\nFor the six quick worked examples (Ohm, voltage divider, RC time constant, dissipated power, RLC resonance, RC-charge ODE), see Worked Examples.","title":"References"},{"category":"section","location":"code-generation/#Code-Generation","page":"Code Generation","text":"Milestone M7 turns the symbolic pipeline into something deployable: compiled Julia evaluators for hot-loop Monte Carlo and calibration-batch workflows, and C source strings for embedded metrology systems. Two new functions plus one escape hatch and one stub:\n\nFunction Purpose\nbuild_evaluator Compile (val, err) to a Julia callable or C source\nto_expr Extract the (val, err) tuple for downstream symbolic tooling\nlatex (M7 stub) Raises ArgumentError pointing at Latexify.jl (full M8)","title":"Code Generation"},{"category":"section","location":"code-generation/#build_evaluator-—-Julia-target","page":"Code Generation","text":"using Symbolics, SymbolicUncertainties\n\n@variables V I σV σI\nR_m = (V ± σV) / (I ± σI)\n\ng = build_evaluator(R_m, [V, I, σV, σI])\nval, err = g(5.0, 0.5, 0.01, 0.001)\n# val ≈ 10.0, err ≈ 0.0282842712474619…\n\nThe returned callable is compiled via Symbolics.build_function with expression = Val{false} — runtime-compiled, sub-100 ns per call on typical GUM measurements. Use it in Monte Carlo loops, calibration- batch processing, and laboratory-automation inner loops where the substitute + toexpr + eval round-trip is too slow.","title":"build_evaluator — Julia target"},{"category":"section","location":"code-generation/#Empty-variables-fast-path","page":"Code Generation","text":"For fully-numeric measurements, pass an empty variable vector:\n\nm = 5.0 ± 0.1\ng = build_evaluator(m, Symbolics.Num[])\ng()   # (5.0, 0.1)","title":"Empty-variables fast path"},{"category":"section","location":"code-generation/#build_evaluator-—-C-source-for-embedded-deployment","page":"Code Generation","text":"c_source = build_evaluator(\n    R_m,\n    [V, I, σV, σI];\n    target = CTarget(),\n    fname = :ohms_law_evaluator,\n)\n\nwrite(\"ohms_law.c\", c_source)\n\nWrites a C file containing:\n\n#include <math.h>\nvoid ohms_law_evaluator(double *du,\n    const double V, const double I,\n    const double σV, const double σI)\n{\n    du[0] = V / I;\n    du[1] = sqrt(pow(σV / I, 2) + pow((V * σI) / (I * I), 2));\n}\n\nCompile it into firmware, PLC toolchains, or regulated- industry deployment builds where the audit trail requires the GUM formula in an approved artefact.","title":"build_evaluator — C source for embedded deployment"},{"category":"section","location":"code-generation/#Scope-note-—-FortranTarget-not-supported-at-M7","page":"Code Generation","text":"EARS REQ-071 mentions \"CTarget or FortranTarget when available\". Symbolics 7 does not export FortranTarget — see upstream-bugs.md UB-004. M7 supports JuliaTarget() and CTarget() only. Requests for other targets raise ArgumentError listing the supported set.\n\nUsers who need Fortran bindings can wrap the C output via iso_c_binding or translate the emitted source manually.","title":"Scope note — FortranTarget not supported at M7"},{"category":"section","location":"code-generation/#to_expr-—-escape-hatch-for-downstream-pipelines","page":"Code Generation","text":"(v, e) = to_expr(R_m)\n# v, e are Symbolics.Num — pipe to ModelingToolkit.jl, a\n# hand-written LaTeX template, or your own code generator.\n\nto_expr returns the (m.val, m.err) pair. The m.dof field is not included — users who need it read m.dof directly.","title":"to_expr — escape hatch for downstream pipelines"},{"category":"section","location":"code-generation/#latex(m)-—-M7-stub","page":"Code Generation","text":"latex(R_m)   # not executed here: this block shows the error itself\n# ┌ ArgumentError: latex(m): full LaTeX rendering requires\n# │ `Latexify.jl`. Load it via `using Latexify` — the M8\n# │ SymbolicUncertaintiesLatexifyExt will provide the closed-\n# │ form implementation. For immediate LaTeX output, use\n# │ the M4 `Base.show(io, MIME\"text/latex\", m)` method …\n# └\n\nM7 ships latex(m) as a stub so downstream code can reference the eventual name today without getting an UndefVarError. The full implementation lands as an SymbolicUncertaintiesLatexifyExt package extension in M8 (mirroring the M2.5 Giac pattern).\n\nFor immediate LaTeX output today, use either:\n\nThe M4 Base.show(io, MIME\"text/latex\", m) method — emits $val \\pm err$ automatically in Jupyter / Pluto.\nto_expr(m) + a handwritten LaTeX template.","title":"latex(m) — M7 stub"},{"category":"section","location":"code-generation/#Calibration-certificate-snippet-(hand-rolled-example)","page":"Code Generation","text":"@variables V I σV σI\nR = (V ± σV) / (I ± σI)\n\n# M7 building blocks:\n(v, e) = to_expr(R)\ng_compiled = build_evaluator(R, [V, I, σV, σI])\nc_source = build_evaluator(R, [V, I, σV, σI]; target = CTarget())\n\n# Hand-rolled certificate line:\ncertificate_line = \"\\\\[ R = $(v) \\\\pm $(e) \\\\]\"\n# (When M8 lands: certificate_line = latex(R))","title":"Calibration-certificate snippet (hand-rolled example)"},{"category":"section","location":"code-generation/#Performance","page":"Code Generation","text":"The compiled Julia evaluator achieves sub-100 ns per call on typical GUM measurements — at least two orders of magnitude faster than the substitute + toexpr + eval path. The speedup is documented but not CI-gated; users verify with @btime from BenchmarkTools.jl.","title":"Performance"},{"category":"section","location":"code-generation/#Error-paths","page":"Code Generation","text":"Unsupported target (e.g. :bogus or FortranTarget) → ArgumentError listing JuliaTarget / CTarget and noting the FortranTarget deferral per UB-004.\nEmpty variables + non-numeric measurement → Symbolics.build_function error propagates.\nlatex(m) called without Latexify.jl → ArgumentError (M7 stub behaviour; resolved by M8).","title":"Error paths"},{"category":"section","location":"code-generation/#API-reference","page":"Code Generation","text":"","title":"API reference"},{"category":"function","location":"code-generation/#SymbolicUncertainties.build_evaluator","page":"Code Generation","text":"build_evaluator(m, variables; target = JuliaTarget(), fname = :evaluate_measurement)\n\nCompile the (m.val, m.err) computation of the SymbolicMeasurement m into a Julia callable (default) or emit C source as a String, depending on target.\n\nArguments\n\nm::SymbolicMeasurement — the measurement to compile.\nvariables::AbstractVector{<:Num} — the symbolic input variables in positional order.\ntarget (keyword, default JuliaTarget()) — code- generation target. Supported: JuliaTarget(), CTarget(). Any other value raises ArgumentError.\nfname (keyword, default :evaluate_measurement) — name of the generated C function. Ignored for JuliaTarget.\n\nReturns\n\ntarget = JuliaTarget() — a Julia callable g(args...) -> Tuple{Float64, Float64} accepting the variable values positionally and returning (val, err). Runtime-compiled (expression = Val{false}), sub-100 ns per call on typical GUM measurements.\ntarget = CTarget() — a String containing a C function definition. Begins with #include <math.h> and defines void <fname>(double *out, const double arg1, …).\n\nSpecial cases\n\nWhen variables is empty and m is fully numeric (both m.val and m.err unwrap to a plain Real), returns a zero-argument closure yielding the numeric (val, err) pair.\n\nErrors\n\nUnsupported target (including FortranTarget — Symbolics.jl 7 does not export it, see upstream-bugs.md UB-004) → ArgumentError listing supported targets.\nSymbolics.build_function errors (e.g. variables missing from the expression) are re-raised unchanged.\n\nImplements the deployment pathway of JCGM 100:2008 §9. Traces REQ-070 (Julia target) and REQ-071 (C target).\n\n\n\n\n\n","title":"SymbolicUncertainties.build_evaluator"},{"category":"function","location":"code-generation/#SymbolicUncertainties.to_expr","page":"Code Generation","text":"to_expr(m::SymbolicMeasurement) -> Tuple{Num, Num}\n\nReturn the (m.val, m.err) pair for downstream symbolic manipulation. Destructurable via (v, e) = to_expr(m).\n\nThe m.dof field is not included — users who need it read m.dof directly. Export utility for downstream reporting per JCGM 100:2008 §7.\n\nTraces REQ-073.\n\n\n\n\n\n","title":"SymbolicUncertainties.to_expr"},{"category":"function","location":"code-generation/#SymbolicUncertainties.latex","page":"Code Generation","text":"latex(m::SymbolicMeasurement) -> String\n\nnote: M7 stub\nThe full latex(m) implementation ships as an SymbolicUncertaintiesLatexifyExt package extension in Milestone M8. Until then, this function raises ArgumentError with a message pointing at Latexify.jl and the M4 Base.show(io, MIME\"text/latex\", m) method as the current notebook-rendering alternative.\n\nReturn a LaTeX-formatted String representation of the measurement suitable for direct inclusion in a calibration certificate (planned M8 behaviour). Matches the reporting posture of JCGM 100:2008 §7 (reporting uncertainty).\n\nTraces REQ-072 (deferred to M8).\n\n\n\n\n\n","title":"SymbolicUncertainties.latex"},{"category":"section","location":"uncertainty-budget/#Uncertainty-Budget","page":"Uncertainty Budget","text":"The uncertainty budget is the per-variable breakdown of the combined standard uncertainty u_c(y). It is the EA-4/02 §7.3 deliverable of every calibration report — one row per input variable, showing how much each contributes and how sensitive the measurand is to it.\n\nSymbolicUncertainties.jl provides four purely-symbolic helpers that compose cleanly: sensitivity_coefficient, uncertainty_contribution, relative_sensitivity, and the one-call uncertainty_budget that assembles all three into the EA-4/02 §7.3 table.\n\nThe governing normative sections are JCGM 100:2008 §5.1.3 equations (11a) and (11b), and EA-4/02 §7.3 for the row layout.","title":"Uncertainty Budget"},{"category":"section","location":"uncertainty-budget/#Per-variable-helpers","page":"Uncertainty Budget","text":"sensitivity_coefficient(m, xᵢ) returns the symbolic cᵢ = ∂(m.val)/∂xᵢ via Symbolics.derivative. It returns Num(0) when the variable does not appear in the measurand.\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables Vin σVin R1 σR1 R2 σR2\nVout = propagate(\n    (vin, r1, r2) -> vin * r2 / (r1 + r2),\n    [Vin ± σVin, R1 ± σR1, R2 ± σR2],\n)\n\nsensitivity_coefficient(Vout, Vin)  # R2/(R1+R2)\nsensitivity_coefficient(Vout, R1)   # -Vin·R2/(R1+R2)²\nsensitivity_coefficient(Vout, R2)   #  Vin·R1/(R1+R2)²\n\nuncertainty_contribution(m, xᵢ, σᵢ) returns |cᵢ|·σᵢ — the individual uncertainty contribution of xᵢ.\n\nrelative_sensitivity(m, xᵢ, σᵢ) returns (cᵢ·σᵢ)² / u_c²(y) — the EA-4/02 §7.3 \"percentage-of-variance\" column. Summing the relative sensitivities across all uncorrelated input variables and applying Symbolics.simplify returns 1.","title":"Per-variable helpers"},{"category":"section","location":"uncertainty-budget/#The-one-call-budget-table-—-uncertainty_budget","page":"Uncertainty Budget","text":"uncertainty_budget(m, variables, sigmas) returns a Vector{NamedTuple} whose rows follow the EA-4/02 §7.3 layout:\n\nField Meaning\nvariable The input variable xᵢ\nsigma Its standard uncertainty u(xᵢ)\nsensitivity cᵢ = ∂(m.val)/∂xᵢ (JCGM 100:2008 §5.1.3 eq (11b))\ncontribution uᵢ(y) = abs(cᵢ)·σᵢ (JCGM 100:2008 §5.1.3 eq (11a))\nrelative (cᵢ·σᵢ)² / u_c²(y) (EA-4/02 §7.3 \"%-of-variance\" column)\n\nbudget = uncertainty_budget(\n    Vout,\n    [Vin, R1, R2],\n    [σVin, σR1, σR2],\n)\n\n# budget[1].variable == Vin\n# budget[1].sensitivity == R2/(R1+R2)\n# budget[1].contribution == abs(R2/(R1+R2)) · σVin\n# budget[1].relative == (R2/(R1+R2))² · σVin² / Vout.err²","title":"The one-call budget table — uncertainty_budget"},{"category":"section","location":"uncertainty-budget/#Variance-decomposition-invariant-(SC-002)","page":"Uncertainty Budget","text":"For uncorrelated inputs, the sum of the relative sensitivities across the complete input list simplifies to 1:\n\nSymbolics.simplify(sum(row.relative for row in budget))\n# → 1\n\nThis is asserted by test/budget/test_variance_decomposition.jl for the linear, product, and voltage-divider cases, and by the M3 exit-gate test test/examples/test_budget_voltage_divider.jl for the full three-input voltage divider.","title":"Variance-decomposition invariant (SC-002)"},{"category":"section","location":"uncertainty-budget/#Error-handling","page":"Uncertainty Budget","text":"length(variables) != length(sigmas) raises DimensionMismatch.\nA variable that does not appear in m.val produces a row with sensitivity = 0, contribution = 0, relative = 0 — not an error.\nA measurand whose derivative the symbolic engine cannot resolve propagates the REQ-021 ArgumentError from sensitivity_coefficient, directing the user at the apply(f, m; derivative = ...) escape hatch introduced in M2.","title":"Error handling"},{"category":"section","location":"uncertainty-budget/#Dominant-source-—-dominant_source","page":"Uncertainty Budget","text":"dominant_source(m, variables, sigmas; values=nothing) returns the input variable whose variance contribution dominates the budget — the EA-4/02 §7.3 convention for \"where should I focus calibration effort?\" Call without values to get a symbolic first-variable-wins result (with an @info diagnostic about the order-dependence), or pass a substitution dictionary for a numeric argmax:\n\ndom = dominant_source(\n    Vout,\n    [Vin, R1, R2],\n    [σVin, σR1, σR2];\n    values = Dict(\n        Vin => 5.0, R1 => 1_000.0, R2 => 3_000.0,\n        σVin => 0.01, σR1 => 10.0, σR2 => 10.0,\n    ),\n)\n# dom.index, dom.variable, dom.contribution, dom.ranked_by\n\nWhen every sensitivity coefficient is structurally zero (the measurand does not depend on any variable in the list), the function returns (index=0, …) and emits a @warn.","title":"Dominant source — dominant_source"},{"category":"section","location":"uncertainty-budget/#Rendering-the-budget","page":"Uncertainty Budget","text":"Because the return type is plain Vector{NamedTuple}, it composes with every Julia table-rendering library without pulling in a dependency. For example, once the user has loaded DataFrames.jl and PrettyTables.jl themselves:\n\nusing DataFrames, PrettyTables\npretty_table(DataFrame(budget))\n\nThe SymbolicUncertainties.jl runtime has no hard dependency on either package.","title":"Rendering the budget"},{"category":"section","location":"uncertainty-budget/#API-reference","page":"Uncertainty Budget","text":"","title":"API reference"},{"category":"function","location":"uncertainty-budget/#SymbolicUncertainties.sensitivity_coefficient","page":"Uncertainty Budget","text":"sensitivity_coefficient(m::SymbolicMeasurement, xᵢ::Num) -> Num\n\nReturn the symbolic sensitivity coefficient cᵢ = ∂(m.val)/∂xᵢ per JCGM 100:2008 §5.1.3 equation (11b).\n\nIf xᵢ does not appear in m.val, returns Num(0). If the symbolic engine cannot compute a closed-form derivative, raises ArgumentError directing the user at apply(f, m; derivative = ...) (consistent with the M2 propagate failure pathway — REQ-021).\n\nImplements the methodology of JCGM 100:2008 §5.1.3 equation (11b). Traces REQ-040.\n\n\n\n\n\n","title":"SymbolicUncertainties.sensitivity_coefficient"},{"category":"function","location":"uncertainty-budget/#SymbolicUncertainties.uncertainty_contribution","page":"Uncertainty Budget","text":"uncertainty_contribution(m::SymbolicMeasurement, xᵢ::Num, σᵢ::Num) -> Num\n\nReturn the symbolic uncertainty contribution uᵢ(y) = |cᵢ| · σᵢ of variable xᵢ to measurement m, per JCGM 100:2008 §5.1.3 equation (11a).\n\nPresented as a row of an EA-4/02 §7.3 uncertainty-budget table. Traces REQ-041.\n\n\n\n\n\n","title":"SymbolicUncertainties.uncertainty_contribution"},{"category":"function","location":"uncertainty-budget/#SymbolicUncertainties.relative_sensitivity","page":"Uncertainty Budget","text":"relative_sensitivity(m::SymbolicMeasurement, xᵢ::Num, σᵢ::Num) -> Num\n\nReturn the fractional variance contribution (cᵢ · σᵢ)² / u_c²(y) — the EA-4/02 §7.3 \"percentage-of-variance\" column (JCGM 100:2008 §5.1.6).\n\nFor uncorrelated inputs, summing relative_sensitivity over the full input set simplifies to 1 under Symbolics.simplify — the variance-decomposition invariant (REQ-045 / REQ-152).\n\nTraces REQ-042.\n\n\n\n\n\nrelative_sensitivity(m::SymbolicMeasurement, s::SourceId)\n\nFraction of the combined variance contributed by the independent source s (JCGM 100:2008 §5.1.2).\n\nThis is the source-based form, and the one that stays meaningful under correlation: contributions are keyed by the independent measurements the quantity actually derives from, so with independent sources they sum to exactly 1.\n\nThe variable-based method relative_sensitivity(m, xᵢ, σᵢ) remains for models expressed in user symbols, but refuses to answer once correlations are declared, because (cᵢσᵢ)²/u_c² is then no longer a decomposition.\n\nTraces REQ-042, REQ-152, REQ-206.\n\n\n\n\n\n","title":"SymbolicUncertainties.relative_sensitivity"},{"category":"function","location":"uncertainty-budget/#SymbolicUncertainties.uncertainty_budget","page":"Uncertainty Budget","text":"uncertainty_budget(m, variables, sigmas) -> Vector{NamedTuple}\n\nProduce the EA-4/02 §7.3 uncertainty-budget table for the measurement m.\n\nEach row is a NamedTuple with fields (variable, sigma, sensitivity, contribution, relative), where:\n\nvariable — the input variable xᵢ.\nsigma — its standard uncertainty u(xᵢ).\nsensitivity — cᵢ = ∂f/∂xᵢ via Symbolics.derivative (JCGM 100:2008 §5.1.3 equation (11b)).\ncontribution — uᵢ(y) = |cᵢ|·σᵢ (JCGM 100:2008 §5.1.3 equation (11a)).\nrelative — (cᵢ·σᵢ)² / u_c²(y) (EA-4/02 §7.3 \"percentage-of-variance\" column).\n\nInvariants:\n\nRows are ordered to match variables.\nFor uncorrelated inputs, sum(row.relative for row in budget) simplifies to 1 via Symbolics.simplify (REQ-045 / REQ-152).\nVariables not appearing in m.val produce rows with sensitivity = 0, contribution = 0, relative = 0 — not an error.\n\nErrors:\n\nlength(variables) != length(sigmas) raises DimensionMismatch.\nAn unresolved symbolic differential in any per-row sensitivity computation propagates the REQ-021 ArgumentError from sensitivity_coefficient.\n\nWhen DataFrames.jl is loaded, the SymbolicUncertaintiesDataFramesExt package extension overloads this function to return a DataFrame with the same column layout (REQ-044).\n\nImplements the methodology of JCGM 100:2008 §5.1.3 and the layout of EA-4/02 §7.3. Traces REQ-044.\n\n\n\n\n\n","title":"SymbolicUncertainties.uncertainty_budget"},{"category":"function","location":"uncertainty-budget/#SymbolicUncertainties.dominant_source","page":"Uncertainty Budget","text":"dominant_source(m::SymbolicMeasurement; values = nothing) -> SourceId\n\nIdentify the independent source contributing the largest share of the combined variance, derived from the quantity's own structure (JCGM 100:2008 §5.1.2, EA-4/02 §7.3).\n\nPass values to substitute numerics before comparing; without it the comparison is only possible when the contributions are already concrete.\n\nTraces REQ-043, REQ-206.\n\n\n\n\n\ndominant_source(m, variables, sigmas; values=nothing) -> NamedTuple\n\nIdentify the variable whose variance contribution dominates.\n\nThe returned NamedTuple has fields (index, variable, contribution, ranked_by):\n\nindex::Int — 1-based position of the dominant variable in variables, or 0 when every sensitivity coefficient is structurally zero.\nvariable::Num — the dominant variable itself.\ncontribution::Num — its uncertainty contribution |cᵢ|·σᵢ.\nranked_by::Symbol — :numeric when a values substitution dictionary was supplied, :symbolic otherwise.\n\nRanking strategy:\n\nWith values = Dict(...): each per-variable contribution is substituted and evaluated to a Float64; the maximum by absolute value is the dominant source.\nWithout values: the ranking is order-dependent (no canonical ordering exists on arbitrary Num expressions). The first variable is returned and an @info message flags the order-dependence.\n\nAll-zero edge case: when every sensitivity coefficient is structurally 0, the record is (index=0, variable=Num(0), contribution=Num(0), ranked_by=:symbolic) and a @warn is emitted.\n\nErrors:\n\nlength(variables) != length(sigmas) raises DimensionMismatch.\n\nTraces REQ-043. The ranking convention follows EA-4/02 §7.3 calibration-report practice; no single GUM section mandates it.\n\n\n\n\n\n","title":"SymbolicUncertainties.dominant_source"},{"category":"section","location":"uncertainty-sources/#Uncertainty-Sources","page":"Uncertainty Sources","text":"Since milestone M11, a SymbolicMeasurement records which independent measurements it derives from, and with what sensitivity, rather than carrying a bare uncertainty number. Every combined uncertainty, sensitivity coefficient, budget and covariance is derived from that one structure.\n\nThis page documents the representation. It is the foundation the propagation operators are being migrated onto; until that migration completes, the binary operators keep their pre-M11 behaviour.","title":"Uncertainty Sources"},{"category":"section","location":"uncertainty-sources/#Why-provenance-is-recorded","page":"Uncertainty Sources","text":"An uncertainty number alone cannot tell you where it came from. Two consequences follow, and both are visible to users:\n\nx - x cannot be known to be exactly zero, because the two operands are indistinguishable once reduced to a pair of numbers;\ntwo quantities that are correlated because they derive from a common upstream measurement cannot be recognised as such, which is precisely the case where a user cannot supply a covariance matrix by hand.\n\nRecording the sources answers both from the same structure.","title":"Why provenance is recorded"},{"category":"section","location":"uncertainty-sources/#Sources-are-carried,-not-registered","page":"Uncertainty Sources","text":"The source descriptors and any declared covariances live in the quantity, not in module-level state.\n\nThis is deliberate. With a global registry, Symbolics.substitute would have nothing local to substitute into: replacing σx with 0.7 would have to reach shared state, and without that a fully substituted measurement would no longer yield a number. Carrying the descriptors keeps substitution purely local — the estimate, each sensitivity, each source uncertainty, each covariance.\n\nThe only global state is a counter that hands out fresh identifiers, which makes ± impure in exactly the way gensym is: bounded, with no leakage between sessions, and safe to call from several threads.\n\nusing Symbolics, SymbolicUncertainties\n@variables V σV\n\nm = V ± σV\nSymbolicUncertainties.terms_of(m)     # one source, sensitivity 1\nSymbolicUncertainties.sources_of(m)   # its standard uncertainty: σV\n\nEach ± mints a fresh source. Two syntactically identical constructions are two independent measurements — the same σ symbol may perfectly well describe two different resistors of equal tolerance.","title":"Sources are carried, not registered"},{"category":"section","location":"uncertainty-sources/#Combined-standard-uncertainty","page":"Uncertainty Sources","text":"The combined standard uncertainty is the quadratic form over the source covariance structure. With independent sources it reduces to JCGM 100:2008 §5.1.2 equation (10); when covariances are declared, the cross terms of §5.2.2 equation (13) appear. The two GUM formulas are two regimes of a single expression rather than two code paths.\n\nm.err is therefore a computed property, not a stored field:\n\nm.err     # σV","title":"Combined standard uncertainty"},{"category":"section","location":"uncertainty-sources/#Correlation","page":"Uncertainty Sources","text":"Two quantities that derive from a common source are correlated, and that correlation is propagated with no matrix supplied by anyone:\n\n@variables V σV R σR\nv = V ± σV\nr = R ± σR\n\np = v * v / r    # power\ni = v / r        # current\n\np / i            # exactly V ± σV — R cancels, V behaves correctly\n\nThis is the case that matters in practice, and the one a user-supplied covariance matrix serves worst: the correlation exists because both quantities descend from the same voltage measurement, so the person writing the model is precisely the person who does not know the covariance.\n\nCorrelation between sources that are not shared — two instruments sharing a reference standard, say — is declared explicitly:\n\n@variables x σx y σy ρ\na = x ± σx\nb = y ± σy\n\na2, b2 = declare_correlated(a, b, ρ)\n(a2 + b2).err        # carries the 2ρσₓσᵧ cross term of eq. (13)\n\ndeclare_correlated returns new quantities and mutates nothing, so two models in the same session can carry different and equally valid hypotheses. The declared covariance enters as the cross term of JCGM 100:2008 §5.2.2 equation (13); with ρ = 0 it collapses onto equation (10). There is one implementation, with two regimes.","title":"Correlation"},{"category":"section","location":"uncertainty-sources/#API","page":"Uncertainty Sources","text":"","title":"API"},{"category":"type","location":"uncertainty-sources/#SymbolicUncertainties.SourceId","page":"Uncertainty Sources","text":"SourceId\n\nIdentity of one independent uncertainty source, per JCGM 100:2008 §4.1 (input quantities Xᵢ whose estimates are the xᵢ).\n\nTwo quantities sharing a SourceId derive from the same physical measurement, which is what makes their covariance computable without a user-supplied matrix.\n\nTraces REQ-200.\n\n\n\n\n\n","title":"SymbolicUncertainties.SourceId"},{"category":"type","location":"uncertainty-sources/#SymbolicUncertainties.Source","page":"Uncertainty Sources","text":"Source\n\nDescription of one independent source: its symbolic standard uncertainty u (JCGM 100:2008 §2.3.1), its optional degrees of freedom (§G.3, used by Welch-Satterthwaite), and a display name.\n\nTraces REQ-200, REQ-208.\n\n\n\n\n\n","title":"SymbolicUncertainties.Source"},{"category":"function","location":"uncertainty-sources/#SymbolicUncertainties.terms_of","page":"Uncertainty Sources","text":"terms_of(m) -> Dict{SourceId,Symbolics.Num}\n\nSensitivity of m to each independent source it derives from.\n\n\n\n\n\n","title":"SymbolicUncertainties.terms_of"},{"category":"function","location":"uncertainty-sources/#SymbolicUncertainties.sources_of","page":"Uncertainty Sources","text":"sources_of(m) -> Dict{SourceId,Source}\n\nDescriptors of the independent sources m derives from.\n\n\n\n\n\n","title":"SymbolicUncertainties.sources_of"},{"category":"function","location":"uncertainty-sources/#SymbolicUncertainties.cov_of","page":"Uncertainty Sources","text":"cov_of(m) -> Dict{Tuple{SourceId,SourceId},Symbolics.Num}\n\nCovariances declared between the sources of m.\n\n\n\n\n\n","title":"SymbolicUncertainties.cov_of"},{"category":"function","location":"uncertainty-sources/#SymbolicUncertainties.declare_correlated","page":"Uncertainty Sources","text":"declare_correlated(m1, m2, ρ) -> (SymbolicMeasurement, SymbolicMeasurement)\n\nDeclare a correlation coefficient ρ between the sources of m1 and m2, returning new quantities carrying that hypothesis.\n\nNothing is mutated. A correlation hypothesis is a property of the measurement model, so it travels with the quantities it concerns rather than acting at a distance on shared state — two models in the same session can hold different, equally valid assumptions.\n\nThe declared covariance u(xᵢ,xⱼ) = ρ · u(xᵢ) · u(xⱼ) enters the combined uncertainty as the cross term of JCGM 100:2008 §5.2.2 equation (13). With ρ = 0 equation (13) collapses onto equation (10), so the independent case is a special case rather than a separate code path.\n\nBoth quantities must derive from a single source each; correlations between composite quantities are declared between their underlying sources.\n\nTraces REQ-203, REQ-205.\n\n\n\n\n\n","title":"SymbolicUncertainties.declare_correlated"},{"category":"section","location":"interoperability/#Interoperability","page":"Interoperability","text":"Three optional package extensions turn SymbolicUncertainties.jl into a hub for broader Julia scientific-computing workflows. Each activates automatically when the triggering package is loaded.","title":"Interoperability"},{"category":"section","location":"interoperability/#Latexify.jl-—-calibration-certificate-LaTeX","page":"Interoperability","text":"using Symbolics, SymbolicUncertainties, Latexify\n\n@variables V I σV σI\nR = (V ± σV) / (I ± σI)\n\nlatex(R)\n# e.g. \"\\\\frac{V}{I} \\\\pm \\\\sqrt{\\\\frac{σV^{2}}{I^{2}} + \\\\frac{V^{2} σI^{2}}{I^{4}}}\"\n\nThe M7 stub's ArgumentError is transparently replaced by the M8 SymbolicUncertaintiesLatexifyExt. Use the returned String directly in your LaTeX calibration-certificate template.","title":"Latexify.jl — calibration-certificate LaTeX"},{"category":"section","location":"interoperability/#Measurements.jl-—-convert-a-substituted-measurement","page":"Interoperability","text":"warning: Load `Measurements` with `import`, not `using`\nMeasurements.jl exports ± as well. using both packages makes the operator ambiguous and unusable — Julia resolves neither. Use import Measurements and qualify the call, the same idiom DynamicQuantities recommends for coexisting with Unitful.\n\nBridge to the standard Julia uncertainty type for numerical pipelines:\n\nusing Symbolics, SymbolicUncertainties\nimport Measurements\n\nm = 5.0 ± 0.1\nMeasurements.Measurement(m)\n# Measurement{Float64}(5.0 ± 0.1)\n\nsubstitute does not auto-promote — the conversion is explicit:\n\n@variables V I σV σI\nR = (V ± σV) / (I ± σI)\nR_num = Symbolics.substitute(R, Dict(V => 5.0, I => 0.5, σV => 0.01, σI => 0.001))\nMeasurements.Measurement(R_num)\n# Measurement{Float64}(10.0 ± 0.028…)\n\nConversion fails with ArgumentError when either m.val or m.err is still symbolic — call substitute first.","title":"Measurements.jl — convert a substituted measurement"},{"category":"section","location":"interoperability/#DataFrames.jl-—-tabular-uncertainty_budget-output","page":"Interoperability","text":"Opt into a DataFrame return type via the as = :dataframe keyword (the default remains :namedtuple to preserve backward compatibility):\n\nusing SymbolicUncertainties, Symbolics, DataFrames\n\n@variables V σV R1 σR1 R2 σR2\nVout = propagate(\n    (v, r1, r2) -> v * r2 / (r1 + r2),\n    [V ± σV, R1 ± σR1, R2 ± σR2],\n)\n\ndf = uncertainty_budget(\n    Vout,\n    [V, R1, R2],\n    [σV, σR1, σR2];\n    as = :dataframe,\n)\n# df isa DataFrame with 3 rows and columns\n#   variable, sigma, sensitivity, contribution, relative\n\nExport to CSV, join with other tabular data, apply filters — the full DataFrames.jl ergonomics are available. When DataFrames.jl is not loaded, requesting as = :dataframe raises ArgumentError.","title":"DataFrames.jl — tabular uncertainty_budget output"},{"category":"section","location":"interoperability/#Combined-workflow","page":"Interoperability","text":"All three extensions compose cleanly:\n\nusing Symbolics, SymbolicUncertainties\nusing Latexify, DataFrames\nimport Measurements\n\n# Build, propagate, report.\n@variables V σV R1 σR1 R2 σR2\nVout = propagate(\n    (v, r1, r2) -> v * r2 / (r1 + r2),\n    [V ± σV, R1 ± σR1, R2 ± σR2],\n)\n\n# Tabular budget.\ndf = uncertainty_budget(\n    Vout,\n    [V, R1, R2],\n    [σV, σR1, σR2];\n    as = :dataframe,\n)\n\n# LaTeX for the certificate.\ntex = latex(Vout)\n\n# Numeric final result.\nVout_num = Symbolics.substitute(\n    Vout,\n    Dict(V => 5.0, R1 => 1_000.0, R2 => 3_000.0,\n         σV => 0.01, σR1 => 1.0, σR2 => 1.0),\n)\nmeas = Measurements.Measurement(Vout_num)","title":"Combined workflow"},{"category":"section","location":"interoperability/#API-reference","page":"Interoperability","text":"The extensions overload methods on existing functions — no new exports are introduced at the SymbolicUncertainties public surface (REQ-132 audit).\n\nlatex — docstring remains the M7 stub text; the M8 extension replaces the method body at load time.\nuncertainty_budget — gains the as = :namedtuple | :dataframe keyword.\nMeasurements.Measurement(m) — a constructor on Measurements.jl's type, visible to users via the external package's namespace.","title":"API reference"},{"category":"section","location":"methodology-reference/#Methodology-Reference","page":"Methodology Reference","text":"A single-page consolidation of the JCGM 100:2008 section pointers for every exported symbol of SymbolicUncertainties.jl v0.10.0. Row order matches EARS Appendix A after the M10 reconciliation (25 rows total).\n\nThe per-function docstrings under src/*.jl are the source of truth for REQ-160 citations; the table below is a navigable consolidation view. Drift between this table and the docstrings is caught by the test/package/test_api_freeze.jl audit (REQ-160 enforcement).","title":"Methodology Reference"},{"category":"section","location":"methodology-reference/#Public-surface","page":"Methodology Reference","text":"Symbol Description JCGM / GUM ref\nSymbolicMeasurement Estimate + combined standard uncertainty + dof. §4.1 (estimate), §5.1 (uc), §G.4 (νeff)\n± Constructor shorthand val ± u. §6 (notation)\napply Scalar function with optional sensitivity coefficient. §5.1.3\npropagate Multi-variable uncertainty propagation (uncorrelated + correlated). §5.1.2 eq (10), §5.2.2 eq (13)\npropagate_vector Vector-valued measurand propagation. JCGM 102:2011\nsensitivity_coefficient Symbolic cᵢ = ∂f/∂xᵢ. §5.1.3\nuncertainty_contribution Symbolic |cᵢ| · uᵢ. §5.1.6\nrelative_sensitivity Fractional variance contribution. §5.1.6\ndominant_source Variable with largest variance contribution. §5.1.6\nuncertainty_budget Full GUM uncertainty budget table. §5.1.6; EA-4/02 §7.3\nexpanded_uncertainty U = k · u_c. §6 (k = 2); §G.1.2 (arbitrary k); Table G.2 (coverage probability)\nwelch_satterthwaite Effective degrees of freedom. §G.4\nrequired_precision Symbolic condition on uᵢ for target u_c. §5.2 (inverse)\nbudget_allocation Lagrange allocation of uncertainty budget. §5.2 (inverse) + Lagrange optimisation\nbuild_evaluator Compile to Julia / C function. — (code generation)\nSymbolics.substitute Numerical substitution — extension method, not a new export. — (numerical evaluation)\ninfer_precision Solve for uᵢ given observed u_c. — (inverse, see §5.2)\ninfer_all_precisions Worst-case single-source attribution. — (inverse, see §5.2)\nlatex LaTeX string for certificates (requires Latexify.jl). §7 (reporting)\nto_expr Extract (val, err) tuple of Num. — (export utility)\npropagate_ode ODE uncertainty propagation (requires ModelingToolkit.jl). GUM Supplement 3 (anticipated)\nuncertainty_ode Augmented ODESystem for numerical integration. GUM Supplement 3 (anticipated)\ncheck_linearity Nonlinearity indicator per input variable. §5.1.1 (linearity note)\nJuliaTarget Symbolics code-generation target (M7 re-export). — (REQ-132 documented exception)\nCTarget Symbolics code-generation target (M7 re-export). — (REQ-132 documented exception)","title":"Public surface"},{"category":"section","location":"methodology-reference/#Notes","page":"Methodology Reference","text":"substitute is accessed as Symbolics.substitute(m, dict) — an extension method on Symbolics.substitute, not a new export from SymbolicUncertainties. The M4 spec captured this decision to avoid the DynamicPolynomials.substitute / ExproniconLite.substitute name clash.\nJuliaTarget and CTarget are re-exports from Symbolics.jl — the two documented exceptions to the REQ-132 \"no-Symbolics-names-leaked\" audit, needed for the build_evaluator(m, variables; target = …) keyword syntax.\npropagate_ode and uncertainty_ode are exported stubs in src/mtk_stubs.jl that raise ArgumentError until ModelingToolkit.jl is loaded; the live methods live in ext/SymbolicUncertaintiesModelingToolkitExt.jl.\nlatex is similarly a stub in src/latex_stub.jl replaced by the SymbolicUncertaintiesLatexifyExt extension when Latexify.jl is loaded.","title":"Notes"},{"category":"section","location":"methodology-reference/#Cross-reference","page":"Methodology Reference","text":"Standards-traceability gate: EARS REQ-160 (per-function citation), REQ-161 (per-topic docs page), REQ-162 (six canonical worked examples — see Worked Examples).\nWaived requirements: see Limitations for the post-1.0 deferral list.\nCitation: CITATION.bib at the repository root.","title":"Cross-reference"},{"category":"section","location":"methodology-reference/#Re-export-docstrings","page":"Methodology Reference","text":"","title":"Re-export docstrings"},{"category":"type","location":"methodology-reference/#SymbolicUncertainties.JuliaTarget","page":"Methodology Reference","text":"JuliaTarget\n\nRe-export of Symbolics.JuliaTarget. Pass to build_evaluator(m, variables; target = JuliaTarget()) to generate a compiled Julia evaluator (the default target). Documented REQ-132 exception — see docs/src/methodology-reference.md.\n\n\n\n\n\n","title":"SymbolicUncertainties.JuliaTarget"},{"category":"type","location":"methodology-reference/#SymbolicUncertainties.CTarget","page":"Methodology Reference","text":"CTarget\n\nRe-export of Symbolics.CTarget. Pass to build_evaluator(m, variables; target = CTarget()) to generate a C source String callable from C / Fortran / any FFI-compatible host. Documented REQ-132 exception — see docs/src/methodology-reference.md.\n\n\n\n\n\n","title":"SymbolicUncertainties.CTarget"},{"category":"section","location":"ode-integration/#ODE-Integration","page":"ODE Integration","text":"The SymbolicUncertaintiesModelingToolkitExt package extension adds two functions, propagate_ode and uncertainty_ode, that propagate parameter uncertainties through dynamical measurement models built on ModelingToolkit.jl. The extension activates automatically when ModelingToolkit is loaded.\n\nThis is the anticipated GUM Supplement 3 use case — calibration models whose measurand is a function of time, such as an RC charge transient, a thermal cool-down, or a chemical dissolution profile.","title":"ODE Integration"},{"category":"section","location":"ode-integration/#Motivation","page":"ODE Integration","text":"GUM §5.1 treats parameters as constants with uncertainty. A dynamical measurement extends that by asking: given p ± σ_p, what is the propagated uncertainty on the state trajectory u(t)?\n\nThe forward-sensitivity approach augments the original ODE with equations for ∂u/∂p:\n\nfracddtleft(fracpartial upartial p_jright)\n= fracpartial fpartial u cdot fracpartial upartial p_j\n+ fracpartial fpartial p_j\n\nThe combined GUM standard uncertainty on u(t) is then\n\nsigma_u(t) = sqrtsum_j\n    left(fracpartial upartial p_jright)^2 sigma_j^2\n\nwhich SymbolicUncertainties.jl constructs symbolically — you never touch the sensitivity derivation by hand.","title":"Motivation"},{"category":"section","location":"ode-integration/#RC-charge-worked-example","page":"ODE Integration","text":"The canonical first-order linear ODE: du/dt = (Vin − u) / (R · C) with u(0) = 0 and three uncertain parameters R, C, Vin.","title":"RC-charge worked example"},{"category":"section","location":"ode-integration/#Build-the-ODESystem","page":"ODE Integration","text":"using ModelingToolkit\nusing ModelingToolkit: t_nounits as t, D_nounits as D, System\nusing SymbolicUncertainties\nusing SymbolicUncertainties: ±\n\n@variables u(t)\n@parameters R C Vin\neqs = [D(u) ~ (Vin - u) / (R * C)]\n@named rc_sys = System(eqs, t)\n\nR_m   = R   ± 0.01R\nC_m   = C   ± 0.01C\nVin_m = Vin ± 0.001Vin","title":"Build the ODESystem"},{"category":"section","location":"ode-integration/#propagate_ode-—-symbolic-snapshot","page":"ODE Integration","text":"result = propagate_ode(rc_sys, [R_m, C_m, Vin_m])\n# result isa Vector{SymbolicMeasurement} of length 1\n# (one state: u(t)).\n\nresult[1].val\n# u(t) — the state variable\nresult[1].err\n# sqrt((0.0001·R²)·(∂u_∂R(t))² + (0.0001·C²)·(∂u_∂C(t))²\n#      + (1.0e-6·Vin²)·(∂u_∂Vin(t))²)\n\nThe err expression is the symbolic sqrt(Σⱼ (∂uᵢ/∂pⱼ)² · σⱼ²) formula with the sensitivity symbols ∂u_∂R(t), ∂u_∂C(t), ∂u_∂Vin(t) left as free variables. They are populated numerically by the augmented-ODE solve below.","title":"propagate_ode — symbolic snapshot"},{"category":"section","location":"ode-integration/#uncertainty_ode-—-augmented-System-for-solve","page":"ODE Integration","text":"augmented = uncertainty_ode(rc_sys, [R_m, C_m, Vin_m])\n# augmented isa System with 1 + 3·1 = 4 states:\n#   u(t), ∂u_∂R(t), ∂u_∂C(t), ∂u_∂Vin(t)\n\nPass directly to OrdinaryDiffEq:\n\nusing OrdinaryDiffEq\n\ncompiled = mtkcompile(augmented)\nsens_states = setdiff(unknowns(augmented), unknowns(rc_sys))\nu0 = Dict(u => 0.0, (s => 0.0 for s in sens_states)...)\np  = Dict(R => 1.0, C => 1e-3, Vin => 5.0)\n\nprob = ODEProblem(compiled, merge(u0, p), (0.0, 50e-3))\nsol  = solve(prob, Tsit5(); abstol = 1e-10, reltol = 1e-10)\n\nsol[u, end]         # numerical u(t_end) ≈ Vin\n# access sensitivities by symbol name too\n\nAt t → ∞ the state converges to Vin, so ∂u/∂Vin → 1 and ∂u/∂R, ∂u/∂C → 0 — the sensitivities carry the transient.","title":"uncertainty_ode — augmented System for solve"},{"category":"section","location":"ode-integration/#When-to-reach-for-which-function","page":"ODE Integration","text":"Goal Use\nA symbolic expression for u(t) and its uncertainty band propagate_ode\nNumerical state + sensitivity trajectories uncertainty_ode → solve\nCalibration-certificate single-point number uncertainty_ode → solve → evaluate GUM formula\n\nBoth share the same _build_sensitivity_eqs internal machinery, so the symbolic content agrees — only the output shape differs.","title":"When to reach for which function"},{"category":"section","location":"ode-integration/#Scope-and-limitations","page":"ODE Integration","text":"ODE-only, not DAE. Milestone M9 targets ODESystems without algebraic constraints. DAE systems are rejected with a clear ArgumentError.\nConstant uncertain parameters. Time-varying or stochastic forcing is outside scope — that is the JCGM 101:2008 Monte Carlo regime; reach for MonteCarloMeasurements.jl + StochasticDiffEq.jl.\nModelingToolkit version. The extension targets MTK 11.x. Breaking MTK releases may require a follow-up patch.\nMissing parameter. If a SymbolicMeasurement.val in uncertain_params is not a parameter of sys, both functions raise ArgumentError naming the offending symbol.","title":"Scope and limitations"},{"category":"section","location":"ode-integration/#API-reference","page":"ODE Integration","text":"","title":"API reference"},{"category":"function","location":"ode-integration/#SymbolicUncertainties.propagate_ode","page":"ODE Integration","text":"propagate_ode(sys, uncertain_params) -> Vector{SymbolicMeasurement}\n\nnote: Extension-provided\nThe full implementation lives in SymbolicUncertaintiesModelingToolkitExt and activates when ModelingToolkit.jl is loaded. Without MTK loaded, this stub raises ArgumentError.\n\nPropagate parameter uncertainties symbolically through a ModelingToolkit.ODESystem. Returns a Vector{SymbolicMeasurement} of length length(unknowns(sys)) — one per state — whose err field is the GUM-combined uncertainty sqrt(Σⱼ (∂uᵢ/∂pⱼ)² · σⱼ²) built from the forward-sensitivity symbolic expressions.\n\nTraces EARS REQ-081 / §10 (GUM Supplement 3, anticipated).\n\n\n\n\n\n","title":"SymbolicUncertainties.propagate_ode"},{"category":"function","location":"ode-integration/#SymbolicUncertainties.uncertainty_ode","page":"ODE Integration","text":"uncertainty_ode(sys, uncertain_params) -> ODESystem\n\nnote: Extension-provided\nThe full implementation lives in SymbolicUncertaintiesModelingToolkitExt and activates when ModelingToolkit.jl is loaded. Without MTK loaded, this stub raises ArgumentError.\n\nReturn an augmented ModelingToolkit.ODESystem with the original N states plus K·N forward-sensitivity states ∂uᵢ/∂pⱼ(t) (state-major, parameter-minor ordering) and their corresponding forward-sensitivity equations. Pass directly to ODEProblem / solve from the SciML stack for numerical integration of both the trajectory and its sensitivities.\n\nTraces EARS REQ-082 / §10 (GUM Supplement 3, anticipated).\n\n\n\n\n\n","title":"SymbolicUncertainties.uncertainty_ode"},{"category":"section","location":"sensitivity-analysis/#Sensitivity-Analysis","page":"Sensitivity Analysis","text":"This page walks through the central API of SymbolicUncertainties.jl for the GUM §5.1 use case: closed-form propagation of measurement uncertainty through a user-supplied Julia function via symbolic sensitivity coefficients. The same machinery powers the elementary-function overloads, the apply escape hatch, the multi-variable propagate, and the vector-valued propagate_vector.\n\nThe relevant normative sections of JCGM 100:2008 are §5.1.2 (equation 10 — uncorrelated propagation), §5.1.3 (equation 11a/b — sensitivity coefficients cᵢ ≡ ∂f/∂xᵢ and individual contributions uᵢ(y) = |cᵢ|·u(xᵢ)), and §5.2.2 (equation 13 — correlated propagation with cross-covariance terms). For vector-valued measurands, JCGM 102:2011 (GUM Supplement 2) is the governing document.","title":"Sensitivity Analysis"},{"category":"section","location":"sensitivity-analysis/#Elementary-functions-on-a-single-measurement","page":"Sensitivity Analysis","text":"Sixteen Base mathematical functions accept a single SymbolicMeasurement and return a propagated measurement:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables x σx\nm = x ± σx\n\nsin(m)        # u(sin(x)) = |cos(x)| · σx\ncos(m)        # u(cos(x)) = |sin(x)| · σx\nexp(m)        # u(exp(x)) = exp(x) · σx\nlog(m)        # u(log(x)) = σx / |x|\nsqrt(m)       # u(√x)     = σx / (2·√x)\ninv(m)        # u(1/x)    = σx / x²\n\nThe full list is sin, cos, tan, asin, acos, atan, sinh, cosh, tanh, exp, log, log2, log10, sqrt, abs, inv. Each propagates uncertainty via the standard sensitivity-coefficient formula u(f(x)) = |∂f/∂x| · u(x) from JCGM 100:2008 §5.1.2 (single-input specialisation).\n\nWhen the symbolic differentiation engine cannot find a closed-form derivative (e.g. for an iterative numerical solver wrapped as a Julia function, or for an unknown special function), the call raises an ArgumentError directing you at the apply escape hatch described below.","title":"Elementary functions on a single measurement"},{"category":"section","location":"sensitivity-analysis/#propagate-—-the-central-multi-variable-API","page":"Sensitivity Analysis","text":"propagate(f, [m₁, m₂, …, mₙ]) evaluates the user function f symbolically on the input estimates [m.val for m in ms], computes all sensitivity coefficients cᵢ = ∂f/∂xᵢ via Symbolics.derivative, and returns a new SymbolicMeasurement with the combined standard uncertainty per JCGM 100:2008 §5.1.2:\n\nu_c²(y) = sum_i=1^N left(fracpartial fpartial x_iright)^2 u^2(x_i)\n\nA two-input addition:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables a σa b σb\na_m = a ± σa\nb_m = b ± σb\n\nresult = propagate((x, y) -> x + y, [a_m, b_m])\n# val: a + b\n# err: sqrt(σa² + σb²)\n\nA nonlinear three-input combination follows the same shape — every sensitivity coefficient is computed automatically:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables a σa b σb c σc\nresult = propagate(\n    (x, y, z) -> x * y * z,\n    [a ± σa, b ± σb, c ± σc],\n)\n# err: sqrt((b·c·σa)² + (a·c·σb)² + (a·b·σc)²)","title":"propagate — the central multi-variable API"},{"category":"section","location":"sensitivity-analysis/#Worked-example-—-voltage-divider","page":"Sensitivity Analysis","text":"The textbook voltage divider Vout = Vin · R₂ / (R₁ + R₂) is a three-input non-linear measurand. With distinct symbolic inputs:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables Vin σVin R1 σR1 R2 σR2\n\nVin_m = Vin ± σVin\nR1_m  = R1 ± σR1\nR2_m  = R2 ± σR2\n\nVout = propagate(\n    (Vin, R1, R2) -> Vin * R2 / (R1 + R2),\n    [Vin_m, R1_m, R2_m],\n)\n\nThe sensitivity coefficients are:\n\nfracpartial V_outpartial V_in = fracR_2R_1 + R_2\nqquad\nfracpartial V_outpartial R_1 = -fracV_in R_2(R_1 + R_2)^2\nqquad\nfracpartial V_outpartial R_2 = fracV_in R_1(R_1 + R_2)^2\n\nand the propagated uncertainty is\n\nu(V_out) = sqrt\n  left(fracR_2R_1+R_2right)^2 sigma_V_in^2 +\n  left(fracV_inR_2(R_1+R_2)^2right)^2 sigma_R_1^2 +\n  left(fracV_inR_1(R_1+R_2)^2right)^2 sigma_R_2^2\n\n\nVout.err matches this expression up to symbolic simplification and is asserted exactly by test/examples/test_voltage_divider.jl, the M2 exit-gate test.\n\nThe voltage divider is not one of the worked examples in JCGM 100:2008 Annex H (which lists end-gauge calibration in H.1, simultaneous resistance-and-reactance measurement in H.2, thermometer calibration in H.3, etc.). It is retained here as a standard textbook illustration of the product / quotient rule.","title":"Worked example — voltage divider"},{"category":"section","location":"sensitivity-analysis/#Identity-cancellation","page":"Sensitivity Analysis","text":"An expression that reuses the same measurement cancels exactly, through the plain operators:\n\njulia> m = 8.4 ± 0.7\n8.4 ± 0.7\n\njulia> m - m\n0.0 ± 0\n\njulia> m / m\n1 ± 0\n\njulia> m + m\n16.8 ± sqrt(1.9599999999999997)\n\nUntil milestone M11 the operators combined the .err fields of their operands without knowing whether the two were the same measurement, so m - m reported σ√2 and propagate was the documented way round it. Identity now lives in the type, and propagate produces the same answers because it is a convenience over those operators rather than a separate path:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables x σx\nm = x ± σx\n\npropagate(y -> y - y,         [m])    # → 0 ± 0\npropagate(y -> y / y,         [m])    # → 1 ± 0\npropagate(y -> y*y*y - y^3,   [m])    # → 0 ± 0\n\nSince M11 the plain binary operators do this too — there is no longer a right and a wrong way to write the same model:\n\nm - m        # → 0 ± 0\nm / m        # → 1 ± 0\nm + m        # → 2x ± 2σx, not σ√2\n\npropagate remains available for models expressed as a function of several inputs, but it is now a convenience over those operators rather than a separate, more correct path. The regression tests are test/examples/test_identity_cancellation.jl and test/chainrule/test_operand_identity.jl.\n\nBefore M11 the operators applied the uncorrelated formula to each operand's uncertainty number, which could not know whether two operands were the same measurement; propagate was the documented workaround. See Uncertainty Sources for the representation that removed the distinction.","title":"Identity cancellation"},{"category":"section","location":"sensitivity-analysis/#apply-—-the-escape-hatch","page":"Sensitivity Analysis","text":"When the symbolic differentiation engine cannot find a closed-form derivative for the user's function f, calling f(m) directly or propagate(f, [m]) raises ArgumentError whose message names the function and points at apply:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n# Declare a function whose derivative Symbolics cannot find.\n@register_symbolic my_special_function(x)\n\n@variables x σx\nm = x ± σx\n\n# Calling propagate (or the function directly) on a measurement\n# raises ArgumentError because Symbolics has no derivative rule.\n# The error message tells you to use `apply`.\n\n# Supply the derivative explicitly as a symbolic expression in m.val:\nmy_df = 2 * x   # whatever the closed-form derivative is\nresult = apply(my_special_function, m; derivative = my_df)\n# val: my_special_function(x)\n# err: |2x| · σx\n\nThe derivative keyword takes a symbolic expression in the same variable as m.val, not a Julia function. To use a function-style derivative, evaluate it at m.val first:\n\ndf_fn(x) = 2 * x\napply(my_special_function, m; derivative = df_fn(m.val))\n\nWhen called without the derivative keyword, apply(f, m) delegates to propagate(f, [m]) and uses Symbolics.derivative in the normal way. This makes apply a strict generalisation of propagate(f, [m]) that adds the explicit-derivative override.","title":"apply — the escape hatch"},{"category":"section","location":"sensitivity-analysis/#Correlated-inputs-—-propagate(f,-ms,-Σ)","page":"Sensitivity Analysis","text":"When the input quantities are correlated, supply a covariance matrix Σ as the third argument. The library applies the correlated form of the law of propagation, JCGM 100:2008 §5.2.2 equation (13), including all cross-covariance terms:\n\nu_c²(y) = sum_ij fracpartial fpartial x_i\n                       fracpartial fpartial x_j  u(x_i x_j)\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables x σx y σy ρ\n\nx_m = x ± σx\ny_m = y ± σy\n\n# Symbolic 2×2 covariance matrix with correlation coefficient ρ.\nΣ = [σx^2  ρ*σx*σy\n     ρ*σx*σy  σy^2]\n\nresult = propagate((a, b) -> a + b, [x_m, y_m], Σ)\n# val: x + y\n# err: sqrt(σx² + σy² + 2·ρ·σx·σy)\n\nWrong-shape covariance matrices raise DimensionMismatch:\n\n# 3×3 Σ with a 2-element measurement vector → DimensionMismatch\n# 2×3 non-square Σ                          → DimensionMismatch\n\nWhen Σ is diagonal (off-diagonal entries are zero), the correlated method produces results mathematically equivalent to the uncorrelated propagate(f, [m₁, m₂]). This is the FR-010 strict-generalisation invariant.","title":"Correlated inputs — propagate(f, ms, Σ)"},{"category":"section","location":"sensitivity-analysis/#Vector-valued-measurands-—-propagate_vector","page":"Sensitivity Analysis","text":"For multi-output models, propagate_vector(f, ms) accepts a function f that returns a tuple or vector of derived quantities and returns a Vector{SymbolicMeasurement} whose .err fields come from the symbolic Jacobian:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables r σr θ σθ\nr_m = r ± σr\nθ_m = θ ± σθ\n\n# Polar-to-Cartesian conversion: 2 inputs, 2 outputs.\nms = propagate_vector(\n    (r, θ) -> (r * cos(θ), r * sin(θ)),\n    [r_m, θ_m],\n)\n\n# ms[1].val = r·cos(θ); ms[1].err = sqrt((cos(θ))²·σr² + (r·sin(θ))²·σθ²)\n# ms[2].val = r·sin(θ); ms[2].err = sqrt((sin(θ))²·σr² + (r·cos(θ))²·σθ²)\n\nEach output measurement carries only its marginal propagated uncertainty (the diagonal of J · Σ_in · J^T where J is the symbolic Jacobian and Σ_in is a diagonal matrix of input variances). The full output covariance matrix is not exposed at M2; users who need it can call Symbolics.jacobian(vals, xs) directly and assemble it themselves.\n\nThis implements the methodology of JCGM 102:2011 (GUM Supplement\n\nfor multi-output measurands.","title":"Vector-valued measurands — propagate_vector"},{"category":"section","location":"sensitivity-analysis/#Optional-CAS-grade-simplification-via-Giac.jl","page":"Sensitivity Analysis","text":"All propagated .err fields are routed through an internal hook, _simplify_for_report, which defaults to Symbolics.simplify. The Symbolics rewriter handles polynomial and rational forms well but leaves certain trigonometric or transcendental identities unreduced. For example, Symbolics.simplify(-tan(x) + sin(x)/cos(x)) returns (sin(x) - cos(x)·tan(x)) / cos(x) rather than 0.\n\nLoading Giac.jl activates the SymbolicUncertaintiesGiacExt package extension, which overloads _simplify_for_report to route expressions through Giac's CAS. The override is strictly additive — whenever Giac fails or errors, the default Symbolics.simplify is used instead, so every pre-existing test continues to pass.\n\nusing Symbolics\nusing Giac            # activates the extension\nusing SymbolicUncertainties\n\n@variables x σx\nm = x ± σx\n\n# d/dx[tan(x) − sin(x)/cos(x)] ≡ 0, but the Symbolics default leaves\n# the expression in an unreduced form.\nresult = propagate(y -> tan(y) - sin(y) / cos(y), [m])\nresult.err   # → 0   (with Giac loaded)\n             # → an unsimplified expression (without Giac)\n\nThe extension uses the Giac.to_giac / Giac.to_symbolics round-trip together with Giac's simplify command. Load it whenever sensitivity expressions involve trigonometric, hyperbolic, or transcendental identities that the default engine leaves unreduced.\n\nThe fallback is defensive by design: a future Giac version change or an input expression Giac cannot parse cannot make the simplification step worse than the Symbolics baseline.","title":"Optional CAS-grade simplification via Giac.jl"},{"category":"section","location":"sensitivity-analysis/#API-reference","page":"Sensitivity Analysis","text":"","title":"API reference"},{"category":"function","location":"sensitivity-analysis/#SymbolicUncertainties.apply","page":"Sensitivity Analysis","text":"apply(f, m::SymbolicMeasurement; derivative = nothing) -> SymbolicMeasurement\n\nApply a single-argument function f to a measurement m, returning the propagated measurement.\n\nWhen derivative === nothing (the default), apply delegates to propagate(f, [m]), which uses Symbolics.derivative to compute the sensitivity coefficient. This path produces results identical (after simplification) to calling f(m) directly for any function the symbolic engine can differentiate.\n\nWhen derivative is a Symbolics.Num expression (in the same variable as m.val), apply uses it directly and computes u(f(x)) = |derivative| · u(x), skipping the symbolic differentiation step. This is the escape hatch for functions whose derivative the symbolic engine cannot find — propagate(f, [m]) would raise ArgumentError directing the user here.\n\nThe derivative keyword takes a symbolic expression, not a Julia function. To use a function-style derivative, evaluate it at m.val first:\n\nmy_df(x) = 2*x\napply(f, m; derivative = my_df(m.val))\n\nImplements the methodology of JCGM 100:2008 §5.1.3 (sensitivity coefficients). Traces REQ-022.\n\n\n\n\n\n","title":"SymbolicUncertainties.apply"},{"category":"function","location":"sensitivity-analysis/#SymbolicUncertainties.propagate","page":"Sensitivity Analysis","text":"propagate(f, ms::AbstractVector{SymbolicMeasurement}) -> SymbolicMeasurement\n\nPropagate uncertainty through a user-supplied Julia function f of length(ms) arguments, evaluated on the input measurements ms, implementing JCGM 100:2008 §5.1.2 equation (10) for uncorrelated inputs:\n\nu_c²(y) = Σᵢ (∂f/∂xᵢ)² · u²(xᵢ)\n\nThe sensitivity coefficients ∂f/∂xᵢ are obtained symbolically from Symbolics.derivative evaluated on f(m₁.val, m₂.val, …, mₙ.val). The result is a new SymbolicMeasurement whose val field is the symbolic output expression and whose err field is the propagated combined standard uncertainty (simplified once via Symbolics.simplify).\n\nWhen the user function reuses the same input mᵢ multiple times in its body, symbolic differentiation correctly handles the cancellation. For example, propagate(x -> x - x, [m]) returns a measurement with zero uncertainty — as does m - m written directly, since M11 moved identity tracking into the type.\n\nRaises ArgumentError (REQ-021 / FR-004) if the symbolic engine cannot compute a closed-form derivative for any input. Use the apply(f, m; derivative = ...) escape hatch in that case.\n\nEdge cases:\n\nlength(ms) == 0 returns SymbolicMeasurement(Num(f()), Num(0)) — empty sum has zero variance.\nf returning a value independent of any ms[i] returns zero propagated uncertainty.\n\nImplements the methodology of JCGM 100:2008 §5.1.2 equation (10). Traces REQ-030, REQ-110.\n\n\n\n\n\n","title":"SymbolicUncertainties.propagate"},{"category":"function","location":"sensitivity-analysis/#SymbolicUncertainties.propagate_vector","page":"Sensitivity Analysis","text":"propagate_vector(f, ms::AbstractVector{SymbolicMeasurement}) -> Vector{SymbolicMeasurement}\n\nPropagate uncertainty through a multi-output function f of length(ms) arguments. f(ms[1].val, ..., ms[N].val) must return something collect-able (a Tuple, Vector, or any iterable producing M symbolic outputs).\n\nReturns a Vector{SymbolicMeasurement} of length M, where each output measurement carries:\n\nval — the symbolic output expression\nerr — the marginal propagated standard uncertainty of that output, i.e. the diagonal of J · Σ_in · J^T where J is the M × N symbolic Jacobian and Σ_in is the diagonal matrix of input variances u²(xₖ)\n\nAt M2 the inputs are assumed uncorrelated. The full cross-output covariance matrix J · Σ_in · J^T is not exposed by this method; users who need it can call Symbolics.jacobian(vals, xs) directly and combine it with Σ_in themselves.\n\nImplements the methodology of JCGM 102:2011 (GUM Supplement 2). Traces REQ-033, REQ-110.\n\n\n\n\n\n","title":"SymbolicUncertainties.propagate_vector"},{"category":"method","location":"sensitivity-analysis/#Base.sin-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.sin(m::SymbolicMeasurement)\n\nPropagate sin through a measurement using the sensitivity coefficient ∂sin(x)/∂x = cos(x). Implements JCGM 100:2008 §5.1.2 equation (10) for a single input. Traces REQ-020.\n\n\n\n\n\n","title":"Base.sin"},{"category":"method","location":"sensitivity-analysis/#Base.cos-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.cos(m::SymbolicMeasurement)\n\nPropagate cos through a measurement using ∂cos(x)/∂x = -sin(x). Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.cos"},{"category":"method","location":"sensitivity-analysis/#Base.tan-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.tan(m::SymbolicMeasurement)\n\nPropagate tan through a measurement using ∂tan(x)/∂x = 1 + tan(x)². Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.tan"},{"category":"method","location":"sensitivity-analysis/#Base.asin-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.asin(m::SymbolicMeasurement)\n\nPropagate asin through a measurement using ∂asin(x)/∂x = 1 / sqrt(1 - x²). Assumes |x| < 1. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.asin"},{"category":"method","location":"sensitivity-analysis/#Base.acos-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.acos(m::SymbolicMeasurement)\n\nPropagate acos through a measurement using ∂acos(x)/∂x = -1 / sqrt(1 - x²). Assumes |x| < 1. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.acos"},{"category":"method","location":"sensitivity-analysis/#Base.atan-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.atan(m::SymbolicMeasurement)\n\nPropagate atan through a measurement using ∂atan(x)/∂x = 1 / (1 + x²). Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.atan"},{"category":"method","location":"sensitivity-analysis/#Base.sinh-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.sinh(m::SymbolicMeasurement)\n\nPropagate sinh using ∂sinh(x)/∂x = cosh(x). Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.sinh"},{"category":"method","location":"sensitivity-analysis/#Base.cosh-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.cosh(m::SymbolicMeasurement)\n\nPropagate cosh using ∂cosh(x)/∂x = sinh(x). Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.cosh"},{"category":"method","location":"sensitivity-analysis/#Base.tanh-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.tanh(m::SymbolicMeasurement)\n\nPropagate tanh using ∂tanh(x)/∂x = 1 - tanh(x)². Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.tanh"},{"category":"method","location":"sensitivity-analysis/#Base.exp-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.exp(m::SymbolicMeasurement)\n\nPropagate exp using ∂exp(x)/∂x = exp(x). Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.exp"},{"category":"method","location":"sensitivity-analysis/#Base.log-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.log(m::SymbolicMeasurement)\n\nPropagate the natural logarithm using ∂log(x)/∂x = 1/x. Assumes x > 0. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.log"},{"category":"method","location":"sensitivity-analysis/#Base.log2-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.log2(m::SymbolicMeasurement)\n\nPropagate the base-2 logarithm using ∂log2(x)/∂x = 1 / (ln(2) · x). Assumes x > 0. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.log2"},{"category":"method","location":"sensitivity-analysis/#Base.log10-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.log10(m::SymbolicMeasurement)\n\nPropagate the base-10 logarithm using ∂log10(x)/∂x = 1 / (ln(10) · x). Assumes x > 0. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.log10"},{"category":"method","location":"sensitivity-analysis/#Base.sqrt-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.sqrt(m::SymbolicMeasurement)\n\nPropagate sqrt using ∂sqrt(x)/∂x = 1 / (2·sqrt(x)). Assumes x > 0. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.sqrt"},{"category":"method","location":"sensitivity-analysis/#Base.abs-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.abs(m::SymbolicMeasurement)\n\nPropagate abs using ∂|x|/∂x = sign(x). The sensitivity coefficient is piecewise (+1 for x > 0, -1 for x < 0); since the propagation formula multiplies by |c|, the propagated standard uncertainty is simply m.err for any non-zero x. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.abs"},{"category":"method","location":"sensitivity-analysis/#Base.inv-Tuple{SymbolicMeasurement}","page":"Sensitivity Analysis","text":"Base.inv(m::SymbolicMeasurement)\n\nPropagate inv using ∂(1/x)/∂x = -1/x². Assumes x ≠ 0. Implements JCGM 100:2008 §5.1.2. Traces REQ-020.\n\n\n\n\n\n","title":"Base.inv"},{"category":"section","location":"linearity-check/#Linearity-Check","page":"Linearity Check","text":"The GUM §5.1.1 note reminds the user that the law of propagation of uncertainty is a first-order Taylor approximation valid when higher-order terms are negligible. Milestone M6's check_linearity surfaces this assumption actively: it returns a per-variable dimensionless nonlinearity indicator\n\neta_i = fracpartial^2 f  partial x_i^2 cdot sigma_i^2\n              2 cdot partial f  partial x_i cdot sigma_i\n\n— the ratio of the neglected second-order Taylor term to the retained first-order term. |ηᵢ| ≪ 1 means the linear propagation is safe; |ηᵢ| > 0.1 signals a regime where Monte Carlo methods (JCGM 101:2008, MonteCarloMeasurements.jl) should be used instead.","title":"Linearity Check"},{"category":"section","location":"linearity-check/#The-two-signalling-behaviours","page":"Linearity Check","text":"","title":"The two signalling behaviours"},{"category":"section","location":"linearity-check/#Symbolic-indicator","page":"Linearity Check","text":"using Symbolics, SymbolicUncertainties\n\n@variables x σx\nη = check_linearity(a -> exp(a), [x ± σx])\n# η[σx] is the symbolic expression for ηᵢ.\n\nFor any SymbolicMeasurement-producing function, the diagonal ∂²f/∂xᵢ² and the first derivative ∂f/∂xᵢ are computed via Symbolics.derivative (reusing the M2 _safe_derivative helper). A per-input σᵢ key in the returned Dict{Num, Num} maps to the symbolic ηᵢ.","title":"Symbolic indicator"},{"category":"section","location":"linearity-check/#Runtime-threshold-@warn","page":"Linearity Check","text":"check_linearity(\n    a -> exp(a),\n    [x ± σx];\n    values = Dict(x => 1.0, σx => 0.5),\n)\n# ┌ Warning: check_linearity: |η| = 0.25 exceeds the GUM\n# │ §5.1.1 linearity threshold of 0.1 for variable `σx`.\n# │ The first-order propagation formula may be inadequate\n# │ — consider Monte Carlo methods\n# │ (MonteCarloMeasurements.jl / JCGM 101:2008). (REQ-181)\n# └\n\nThe values keyword triggers a numeric threshold check: for each ηᵢ that evaluates to a concrete Float64 exceeding 0.1, a runtime @warn fires per REQ-181. Entries with partial / un-substituted values are silently skipped (the indicator in the returned dict is still symbolic for manual inspection).","title":"Runtime threshold @warn"},{"category":"section","location":"linearity-check/#No-warning-at-low-nonlinearity","page":"Linearity Check","text":"check_linearity(\n    a -> exp(a),\n    [x ± σx];\n    values = Dict(x => 1.0, σx => 0.1),\n)\n# η = 0.05 — below threshold → no warning.","title":"No warning at low nonlinearity"},{"category":"section","location":"linearity-check/#Worked-example-—-linear-vs-nonlinear-contrast","page":"Linearity Check","text":"@variables x σx\nη_lin = check_linearity(a -> 2a + 3, [x ± σx])\n# η_lin[σx] simplifies to 0 — exact linearisation.\n\nη_nl = check_linearity(a -> exp(a), [x ± σx])\n# η_nl[σx] = σx / 2 — grows with σx.\n\nThis is the M6 REQ-155 exit gate.","title":"Worked example — linear vs nonlinear contrast"},{"category":"section","location":"linearity-check/#No-silent-higher-order-correction-(REQ-182)","page":"Linearity Check","text":"check_linearity is a pure observer: running it does not change any M1–M5 propagation output. Before-and-after comparisons of propagate(f, [m]) are bit-identical irrespective of whether check_linearity has been called.\n\ny1 = propagate(a -> exp(a), [x ± σx])\ncheck_linearity(a -> exp(a), [x ± σx])     # no side effect\ny2 = propagate(a -> exp(a), [x ± σx])\n# y1.val == y2.val and y1.err == y2.err symbolically.\n\nAsserted by test/linearity/test_no_higher_order_correction.jl.","title":"No silent higher-order correction (REQ-182)"},{"category":"section","location":"linearity-check/#Scope-—-diagonal-only","page":"Linearity Check","text":"M6 tracks the diagonal second derivative ∂²f/∂xᵢ² only — not the mixed partials ∂²f/∂xᵢ∂xⱼ. For measurands whose nonlinearity lives in cross terms (strong input coupling), the diagonal indicator will under-report the nonlinearity and Monte Carlo is the recommended method.","title":"Scope — diagonal only"},{"category":"section","location":"linearity-check/#Error-paths","page":"Linearity Check","text":"Unresolved second derivative → ArgumentError with a message noting that M6 does not expose a derivative_2 override (there is no analogue of the M2 apply(f, m; derivative = ...) escape hatch for second derivatives).\nEmpty input list → empty dict, no error, no f call.","title":"Error paths"},{"category":"section","location":"linearity-check/#Silencing-the-M4-safety-warning-chatter","page":"Linearity Check","text":"The ηᵢ formula contains a division by 2·cᵢ·σᵢ, which is usually symbolic — the M4 REQ-140 @warn will fire from within check_linearity. In tests and REPLs this is typically silenced via:\n\nusing Logging\nLogging.with_logger(Logging.NullLogger()) do\n    check_linearity(a -> exp(a), [x ± σx])\nend\n\nThe M6 test suite uses exactly this pattern.","title":"Silencing the M4 safety-warning chatter"},{"category":"section","location":"linearity-check/#API-reference","page":"Linearity Check","text":"","title":"API reference"},{"category":"section","location":"linearity-check/#From-indicator-to-bound","page":"Linearity Check","text":"check_linearity is a heuristic: it evaluates a second-derivative ratio at a point and warns past |η| > 0.1. It is also diagonal-only — it looks at ∂²f/∂xᵢ² and ignores the mixed partials. That gap is not academic:\n\nusing Symbolics\nusing SymbolicUncertainties\n@variables a σa b σb\n\ncheck_linearity(*, [a ± σa, b ± σb])   # η = 0 for both inputs\n\nA product's Hessian has a zero diagonal, so the indicator calls a·b linear. It is not: all of its nonlinearity sits in ∂²(ab)/∂a∂b = 1.\n\nlinearisation_bound answers the question the indicator only gestures at — by how much can the first-order result be wrong? — and answers it rigorously:\n\nThe true worst error over that region is 0.0582, so the bound contains it and stays tight:\n\nlinearisation_bound(exp, [a ± σa]; coverage_factor = 2,\n                    values = Dict(a => 1.0, σa => 0.1))\n\nTaylor's theorem with the Lagrange remainder bounds the error by ½ Σᵢⱼ max|Hᵢⱼ| · (k·uᵢ)(k·uⱼ), with each Hᵢⱼ bounded by interval arithmetic over the coverage region. Mixed partials included.\n\nThe claim this supports is stronger than a Monte Carlo check's. JCGM 101:2008 validates the linearisation by sampling, so it can only speak for the points it drew; a bounded symbolic Hessian certifies the entire region at once.\n\nThe bound is reported, never applied. REQ-182 stands: no higher-order correction is folded into u_c behind your back. And where no rigorous bound exists — a denominator whose interval spans zero, a log reaching zero, an operation with no interval extension — it raises rather than returning a number that cannot be trusted. An unsound bound is worse than none.","title":"From indicator to bound"},{"category":"function","location":"linearity-check/#SymbolicUncertainties.check_linearity","page":"Linearity Check","text":"check_linearity(f, measurements; values = nothing) -> Dict{Num, Num}\n\nReturn per-variable nonlinearity indicators ηᵢ = (∂²f/∂xᵢ²) · σᵢ² / (2 · ∂f/∂xᵢ · σᵢ) for the user-supplied function f and input measurements, following JCGM 100:2008 §5.1.1 note.\n\n|ηᵢ| is a dimensionless measure of how much the second-order Taylor term contributes relative to the first-order term. |ηᵢ| ≈ 0 means the GUM linearisation is exact for that input; |ηᵢ| > 0.1 signals the first-order-only propagation may be inadequate and Monte Carlo methods (JCGM 101:2008, MonteCarloMeasurements.jl) should be considered.\n\nArguments\n\nf — a user-supplied function of length(measurements) arguments returning a single Symbolics.Num-compatible expression.\nmeasurements::AbstractVector{<:SymbolicMeasurement} — the input measurements. Each m.val is passed to f as the corresponding xᵢ; each m.err is the σᵢ in the indicator formula and the dict key in the returned table.\nvalues::Union{Dict, Nothing} (keyword, default nothing) — optional substitution dictionary. When supplied, each ηᵢ is evaluated numerically and a @warn fires per variable whose |ηᵢ| > 0.1 (REQ-181).\n\nReturn\n\nDict{Num, Num} keyed by the σ-variables of the supplied measurements, mapping each σᵢ to its symbolic indicator.\n\nErrors\n\nArgumentError when Symbolics.derivative returns an unresolved Differential wrapper on either the first or second partial — points the user at the apply(f, m; derivative = ...) M2 escape hatch, and notes that M6 does not expose a second-derivative override keyword.\n\nEdge cases\n\nEmpty measurements returns an empty Dict{Num, Num} without calling f.\nStationary points (∂f/∂xᵢ = 0) produce 0/0 or ∞ indicators; numeric NaN / Inf evaluations are silently skipped by the threshold check (no warning fires).\n\nScope\n\nM6 tracks the diagonal second derivative ∂²f/∂xᵢ² only — not the mixed partials ∂²f/∂xᵢ∂xⱼ. For strongly-coupled measurands Monte Carlo remains the recommended method.\n\nImplements the GUM §5.1.1 linearity-assumption diagnostic. Traces REQ-180, REQ-181, REQ-182, REQ-155.\n\n\n\n\n\n","title":"SymbolicUncertainties.check_linearity"},{"category":"function","location":"linearity-check/#SymbolicUncertainties.linearisation_bound","page":"Linearity Check","text":"linearisation_bound(f, measurements; coverage_factor = 2, values = nothing)\n\nRigorous bound on the error the GUM's first-order linearisation makes, over the coverage region of the inputs.\n\nTaylor's theorem with the Lagrange remainder gives\n\nf(x + δ) = f(x) + Σᵢ cᵢδᵢ + ½ Σᵢⱼ Hᵢⱼ(ξ) δᵢδⱼ\n\nfor some ξ in the region. Bounding every Hᵢⱼ there by interval arithmetic bounds the remainder for every point at once:\n\n|R| ≤ ½ Σᵢⱼ max|Hᵢⱼ| · (k·uᵢ)(k·uⱼ)\n\nThe claim this supports is stronger than JCGM 101:2008's: Monte Carlo checks the linearisation by sampling, so it can only say the points it drew were fine. A bounded symbolic Hessian certifies the whole coverage region deterministically.\n\nUnlike M6's check_linearity, the mixed partials are included. That matters: for f = a·b every ∂²f/∂xᵢ² is zero, so a diagonal-only indicator calls a product linear when the entirety of its nonlinearity sits in ∂²f/∂a∂b.\n\ncoverage_factor sets the region half-width in standard uncertainties (k = 2 ≈ 95 % under a normal assumption, JCGM 100:2008 §6.3.2).\n\nThe result is reported, never applied. REQ-182 stands: no higher-order correction is ever folded into u_c behind the user's back.\n\nRaises ArgumentError when no rigorous bound exists — a denominator straddling zero, log of an interval reaching zero, an operation with no interval extension. An unsound bound is worse than none.\n\nImplements the methodology of JCGM 100:2008 §5.1.2 note and §E.3.1. Traces REQ-180, REQ-182.\n\n\n\n\n\n","title":"SymbolicUncertainties.linearisation_bound"},{"category":"section","location":"inverse-inference/#Inverse-Inference","page":"Inverse Inference","text":"The inverse-inference layer answers the central instrument- design question of metrology: what input precision do I need so that my combined standard uncertainty hits a target?\n\nFour closed-form operations cover the common workflow:\n\ninfer_precision(m, σᵢ, target_uc) — solves m.err = target_uc for σᵢ.\ninfer_all_precisions(m, variables, sigmas, target_uc) — the worst-case single-source precision table.\nrequired_precision(m, σᵢ, target_uc) — inequality framing (delegates to infer_precision).","title":"Inverse Inference"},{"category":"section","location":"inverse-inference/#infer_precision-—-headline-solve","page":"Inverse Inference","text":"using Symbolics, SymbolicUncertainties\n\n@variables V I σV σI\nV_m = V ± σV\nI_m = I ± σI\nR = V_m / I_m   # Ohm's-law resistance\n\nσV_needed = infer_precision(R, σV, 0.01)\n# Closed-form σV* such that R.err = 0.01.\n\nSubstitute concrete numerics to read off the target:\n\nstep1 = Symbolics.substitute(σV_needed, Dict(I => 0.5, σI => 0.001))","title":"infer_precision — headline solve"},{"category":"section","location":"inverse-inference/#Why-the-library-does-not-use-Symbolics.solve_for-or-Symbolics.symbolic_solve","page":"Inverse Inference","text":"Symbolics.solve_for only handles linear equations; Ohm's law is quadratic in σV. Symbolics.symbolic_solve is more capable but requires Nemo.jl for polynomial solving beyond the most trivial case, and even with Nemo it can fail on rational-coefficient systems. SymbolicUncertainties.jl M5 uses the closed-form algebraic split that works for every GUM-standard quadratic measurement:\n\nm.err² = cᵢ²·σᵢ² + Σⱼ≠ᵢ (cⱼ·σⱼ)²\n\n  ⇒ σᵢ* = sqrt((target_uc² − Σⱼ≠ᵢ(cⱼ·σⱼ)²) / cᵢ²)\n\nextracted by subtracting the σᵢ → 0 substituted form from m.err² and dividing by σᵢ². No solver dependency; no Nemo.jl requirement. See specs/007-protocol-and-inference/research.md R1/R2 for the design rationale.\n\nMeasurements whose err is not quadratic in σᵢ (e.g. a transcendental sin(σᵢ) term) raise ArgumentError per REQ-091, with a message recommending using Nemo (if you want to try Symbolics.symbolic_solve directly) or numerical root-finding via an external package.","title":"Why the library does not use Symbolics.solve_for or Symbolics.symbolic_solve"},{"category":"section","location":"inverse-inference/#infer_all_precisions-—-worst-case-table","page":"Inverse Inference","text":"table = infer_all_precisions(R, [V, I], [σV, σI], 0.01)\n# table[σV] == 0.01 / |1/I|\n# table[σI] == 0.01 / |V/I²|\n\nEach entry is the precision that input would need alone to drive u_c(y) = target_uc. Uses the closed-form shortcut σᵢ* = target_uc / |cᵢ| (research R4) where cᵢ comes from the M3 sensitivity_coefficient helper.","title":"infer_all_precisions — worst-case table"},{"category":"section","location":"inverse-inference/#RLC-resonance-—-the-M5-exit-gate-(SC-004)","page":"Inverse Inference","text":"@variables L C σL σC\nf0 = propagate(\n    (l, c) -> 1 / (2π * sqrt(l * c)),\n    [L ± σL, C ± σC],\n)\n\nε = 0.001\nσL_star = infer_precision(f0, σL, ε * f0.val)\n# Closed-form σL* such that f0.err == 0.001 · f0.val.\n\nAsserted numerically by test/examples/test_rlc_resonance.jl via two-pass substitution through the AsFloat helper (no Nemo needed).","title":"RLC resonance — the M5 exit gate (SC-004)"},{"category":"section","location":"inverse-inference/#Error-paths-(REQ-091)","page":"Inverse Inference","text":"ArgumentError fires in the following cases:\n\ntarget_uc isa Real && target_uc < 0 — standard uncertainty must be non-negative (GUM §4.3.1).\nσᵢ not in m.err — the measurand does not depend on the supplied input.\nm.err not quadratic in σᵢ — the algebraic split fails. Message recommends using Nemo or numerical root-finding.","title":"Error paths (REQ-091)"},{"category":"section","location":"inverse-inference/#API-reference","page":"Inverse Inference","text":"","title":"API reference"},{"category":"function","location":"inverse-inference/#SymbolicUncertainties.infer_precision","page":"Inverse Inference","text":"infer_precision(m::SymbolicMeasurement, σᵢ, target_uc) -> Num\n\nSolve m.err == target_uc symbolically for σᵢ and return the positive root.\n\nAssumes the GUM-standard quadratic form m.err² = cᵢ²·σᵢ² + (contribution-of-other-σⱼ)² — true for any measurement built via propagate / propagate_correlated / the M1 binary operators. Under this assumption the closed- form σᵢ* is:\n\nσᵢ* = sqrt((target_uc² − Σⱼ≠ᵢ(cⱼ·σⱼ)²) / cᵢ²)\n\nImplementation (no general-purpose polynomial solver required — sidesteps the Symbolics.symbolic_solve ⇒ Nemo.jl dependency chain):\n\nSquares m.err and simplifies (the sqrt wrapper cancels).\nSubstitutes σᵢ → 0 to obtain the sum of other-input contributions.\nSubtracts to isolate the σᵢ contribution cᵢ²·σᵢ².\nExtracts cᵢ² = contribution / σᵢ² via symbolic division.\nComputes σᵢ* = sqrt((target² − others²) / cᵢ²).\n\nErrors:\n\ntarget_uc isa Real && target_uc < 0 → ArgumentError.\nσᵢ not in m.err → ArgumentError.\nm.err is not quadratic in σᵢ (the σᵢ-contribution does not factor as cᵢ²·σᵢ²) → ArgumentError per REQ-091.\n\nImplements the methodology of JCGM 100:2008 §5.2 inverse. Traces REQ-090, REQ-091.\n\n\n\n\n\n","title":"SymbolicUncertainties.infer_precision"},{"category":"function","location":"inverse-inference/#SymbolicUncertainties.infer_all_precisions","page":"Inverse Inference","text":"infer_all_precisions(m, variables, sigmas, target_uc) -> Dict{Num, Num}\n\nProduce the worst-case single-source precision table. For each pair (xᵢ, σᵢ), compute the σᵢ* that would alone drive m.err = target_uc.\n\nUses the closed-form shortcut (research R4):\n\nσᵢ* = target_uc / abs(sensitivity_coefficient(m, xᵢ))\n\nRequires length(variables) == length(sigmas) (DimensionMismatch otherwise). Propagates any sensitivity_coefficient failure unchanged. Inverse form of JCGM 100:2008 §5.2 applied per-input.\n\nTraces REQ-092.\n\n\n\n\n\n","title":"SymbolicUncertainties.infer_all_precisions"},{"category":"section","location":"display-substitute-safety/#Display,-Substitution,-and-Safety","page":"Display, Substitution, and Safety","text":"Milestone M4 introduces three quality-of-life layers on top of the M1–M3 symbolic pipeline: a one-call bridge from symbolic measurements to numeric values, LaTeX rendering for Jupyter and Pluto notebooks, and two runtime safety warnings (division-by-zero and sqrt/log domain).","title":"Display, Substitution, and Safety"},{"category":"section","location":"display-substitute-safety/#substitute(m,-dict)-—-the-symbolic-to-numeric-bridge","page":"Display, Substitution, and Safety","text":"SymbolicUncertainties.jl extends Symbolics.substitute with a method that accepts a SymbolicMeasurement and a replacement dictionary. Every field (val, err, and — when set — dof) is rewritten via the underlying Symbolics.substitute engine:\n\nusing Symbolics\nusing SymbolicUncertainties\n\n@variables Vin σVin R1 σR1 R2 σR2\nVout = propagate(\n    (vin, r1, r2) -> vin * r2 / (r1 + r2),\n    [Vin ± σVin, R1 ± σR1, R2 ± σR2],\n)\n\nVout_numeric = substitute(\n    Vout,\n    Dict(\n        Vin => 5.0, R1 => 1_000.0, R2 => 3_000.0,\n        σVin => 0.01, σR1 => 1.0, σR2 => 1.0,\n    ),\n)\n\nBehaviour:\n\nSilent ignore of extra keys — any Dict key that does not appear in any of the three fields is silently dropped (REQ-122).\nPartial substitution — a Dict covering only a subset of variables leaves the rest symbolic in the returned measurement.\ndof === nothing preserved — no accidental upgrade to a substituted zero.\nNo simplification pass — users who want Giac-level canonicalisation should call Symbolics.simplify on the returned fields themselves.\nNo REQ-005 re-check — a substituted err that simplifies to a concrete negative Real is returned silently. The REQ-005 negativity guard lives in the numeric constructor; substitute is a rewriting operation, not a construction.","title":"substitute(m, dict) — the symbolic-to-numeric bridge"},{"category":"section","location":"display-substitute-safety/#Extracting-numeric-values","page":"Display, Substitution, and Safety","text":"Symbolics.substitute does not numerically evaluate expressions like sqrt(0.05) — they remain symbolic (documented upstream and mirrored in upstream-bugs.md UB-001). To obtain a concrete Float64, use the standard toexpr + eval round-trip:\n\nval_float = Float64(eval(Symbolics.toexpr(Vout_numeric.val)))\nerr_float = Float64(eval(Symbolics.toexpr(Vout_numeric.err)))\n\nThis pattern also lives in the AsFloat test snippet used throughout the M2/M3 test suite.","title":"Extracting numeric values"},{"category":"section","location":"display-substitute-safety/#LaTeX-rendering-for-Jupyter-/-Pluto","page":"Display, Substitution, and Safety","text":"Base.show(io, MIME\"text/latex\", m) emits a LaTeX-math representation wrapped in inline math delimiters $...$:\n\n$3.75 \\pm 0.00625$\n\nJupyter and Pluto recognise the text/latex MIME type and render the output as math automatically — no caller-side wrapping required. The plain-text text/plain display (val ± err in Unicode) is unchanged from M1 for every REPL and non-notebook consumer.\n\nThe in-library formatter is intentionally minimal: for concrete numeric values it prints the Float64 directly; for symbolic expressions it defers to Base.string(expr). Users who need production-grade LaTeX for complex expressions should load Latexify.jl and call latexify on the individual m.val / m.err fields. A dedicated SymbolicUncertaintiesLatexifyExt package extension may land in a later milestone.","title":"LaTeX rendering for Jupyter / Pluto"},{"category":"section","location":"display-substitute-safety/#Safety-warnings-(division-by-zero-and-sqrt-/-log-domain)","page":"Display, Substitution, and Safety","text":"Two runtime warnings surface potential correctness issues at build time:\n\nDivision-by-zero (REQ-140) fires when a division's denominator cannot be proven nonzero, i.e. when the denominator's .val is not a concrete numeric Real.\nDomain (REQ-141) fires when sqrt, log, log2, or log10 is applied to a measurement whose .val cannot be proven strictly positive.\n\nBoth fire uniformly across the M1 binary operator path and the M2 propagate path. Neither halts the computation — the returned SymbolicMeasurement is valid.\n\n@variables a σa b σb\na_m = a ± σa\nb_m = b ± σb\n\na_m / b_m\n# ┌ Warning: Division by `y` whose `.val` is symbolic — the\n# │ denominator cannot be proven nonzero at build time\n# │ (JCGM 100:2008 §5.1, REQ-140). ...\n# └\n\nsqrt(a_m)\n# ┌ Warning: sqrt applied to a measurement whose `.val` is\n# │ symbolic — the argument cannot be proven strictly positive\n# │ at build time (JCGM 100:2008 §5.1, REQ-141). ...\n# └\n\npropagate((x, y) -> x / y, [a_m, b_m])\n# Fires REQ-140 via the propagate syntactic walker.\n\npropagate((x,) -> log(x), [a_m])\n# Fires REQ-141 via the same walker.","title":"Safety warnings (division-by-zero and sqrt / log domain)"},{"category":"section","location":"display-substitute-safety/#When-no-warning-fires","page":"Display, Substitution, and Safety","text":"The library uses a simple, deterministic rule: a value is \"provably safe\" iff its .val unwraps via Symbolics.value to a plain Julia Real passing the appropriate test (nonzero for division, strictly positive for sqrt/log/log2/log10).\n\none_m = 1.0 ± 0.0\na_m / one_m          # no warning — denominator is provably 1.0\n\nfour_m = 4.0 ± 0.1\nSymbolicUncertainties._warn_domain(:sqrt, four_m)\n                     # no warning — argument is provably 4.0 > 0","title":"When no warning fires"},{"category":"section","location":"display-substitute-safety/#Why-the-library-errs-on-the-side-of-over-warning","page":"Display, Substitution, and Safety","text":"Symbolics.jl does not expose an assumptions framework (assume / additionally) — see upstream-bugs.md UB-003. Without an upstream way to declare \"this symbol is known to be positive\", the library cannot distinguish a user who knows σ > 0 from one who genuinely allows any sign. The conservative choice is to warn whenever the value cannot be proven safe, rather than silently skip a genuine domain violation. A future in-library assume_positive! / assume_nonzero! layer is recorded in UB-003 as the long-term fix; it is out of scope for M4.","title":"Why the library errs on the side of over-warning"},{"category":"section","location":"display-substitute-safety/#Silencing-the-warnings","page":"Display, Substitution, and Safety","text":"Three supported options:\n\nSubstitute first. Call substitute(m, Dict(...)) with concrete numeric values for the problematic variables; the .val then unwraps to a Real and the warning does not fire on subsequent operations.\nJulia stdlib toggle. In a REPL or notebook session, import Logging; Logging.disable_logging(Logging.Warn) suppresses all warnings (not just M4's).\nTest context. Wrap the offending call in Test.@test_logs (:warn, r\"REQ-140\") expr (or REQ-141) to assert the warning is expected. This is how the package's own test suite captures the warnings it emits.\n\nThere is no in-library global silencing switch by design — the constitution's purely-symbolic principle and the Julia-ecosystem aversion to mutable global state make a toggle discouraged.","title":"Silencing the warnings"},{"category":"section","location":"display-substitute-safety/#API-reference","page":"Display, Substitution, and Safety","text":"","title":"API reference"},{"category":"method","location":"display-substitute-safety/#SymbolicUtils.substitute-Tuple{SymbolicMeasurement, AbstractDict}","page":"Display, Substitution, and Safety","text":"Symbolics.substitute(m::SymbolicMeasurement, dict::AbstractDict)\n\nRewrite each of m.val, m.err, and m.dof (when not nothing) by applying Symbolics.substitute with the user-supplied replacement dictionary.\n\nBehaviour:\n\nExtra dict keys not present in any of the three fields are silently ignored (REQ-122).\nPartial substitution is supported — unsubstituted variables remain symbolic in the returned measurement.\nm.dof === nothing is preserved (not upgraded to a substituted zero).\nNo simplification pass is applied after substitution; users who want Giac-level canonicalisation should call Symbolics.simplify on the returned fields themselves.\nThe REQ-005 negativity guard on the numeric constructor is not re-applied. A substituted err that simplifies to a concrete negative Real is returned silently — substitute is a rewriting operation, not a constructor.\n\nImplements the methodology of JCGM 100:2008 §5.1 (the measurement-model substitution step) and §7.2.2 (the transition to the calibration-certificate result). Traces REQ-120, REQ-122, REQ-123, REQ-142.\n\n\n\n\n\n","title":"SymbolicUtils.substitute"},{"category":"section","location":"worked-examples/#Worked-Examples","page":"Worked Examples","text":"Six canonical electrical worked examples per EARS REQ-162. Each section follows the same skeleton:\n\nSetup — variables, parameters, physical model.\nPropagation — symbolic sensitivity analysis.\nUncertainty budget — tabular breakdown per JCGM 100:2008 §5.1.6 / EA-4/02.\nNumeric result — Symbolics.substitute with concrete values.\n\nAll six examples come with dedicated test items in test/examples/ (M1–M9 exit gates) or docs/src/ode-integration.md (M9) that assert the exact numeric outputs. Run the full test suite to exercise them end-to-end.\n\n","title":"Worked Examples"},{"category":"section","location":"worked-examples/#1.-Ohm's-law-(GUM-§5.1-canonical)","page":"Worked Examples","text":"Setup. Voltage V ± σV and current I ± σI (independent, uncorrelated measurements). Measurand: resistance R = V / I.\n\nusing SymbolicUncertainties\nusing SymbolicUncertainties: ±\nusing Symbolics\n\n@variables V I σV σI\n\nv = V ± σV\ni = I ± σI\n\nr = v / i\n# r.val = V / I\n# r.err = (V/I) * sqrt((σV/V)² + (σI/I)²)\n\nPropagation. The two operands share no common symbolic source, so the binary-operator path correctly applies JCGM 100:2008 §5.1.2 eq (10) for uncorrelated inputs.\n\nUncertainty budget.\n\nbudget = uncertainty_budget(r, [V, I], [σV, σI])\n# Row per input: variable, sigma, sensitivity,\n#   contribution, relative (fraction of variance)\n\nNumeric result. Substitute V = 5.0, I = 0.5, σV = 0.01, σI = 0.001:\n\ndict = Dict(V => 5.0, I => 0.5, σV => 0.01, σI => 0.001)\nr_num = Symbolics.substitute(r, dict)\n# r_num.val ≈ 10.0\n# r_num.err ≈ 0.02828…  (V/I · sqrt((σV/V)² + (σI/I)²))\n\nVerified in test/examples/test_ohms_law.jl.\n\n","title":"1. Ohm's law (GUM §5.1 canonical)"},{"category":"section","location":"worked-examples/#2.-Voltage-divider-(sensitivity-analysis)","page":"Worked Examples","text":"Setup. Input voltage Vin ± σVin, two resistors R1 ± σR1 and R2 ± σR2. Measurand: Vout = Vin · R2 / (R1 + R2).\n\nusing SymbolicUncertainties, Symbolics\nusing SymbolicUncertainties: ±\n\n@variables Vin σVin R1 σR1 R2 σR2\n\nVout = propagate(\n    (v, r1, r2) -> v * r2 / (r1 + r2),\n    [Vin ± σVin, R1 ± σR1, R2 ± σR2],\n)\n\nPropagation. propagate computes sensitivity coefficients via Symbolics.derivative; repeated operand identity collapses correctly — since M11 this holds through the plain binary operators as well as through propagate.\n\nUncertainty budget.\n\ndf = uncertainty_budget(\n    Vout,\n    [Vin, R1, R2],\n    [σVin, σR1, σR2];\n    as = :namedtuple,\n)\n\nNumeric result. Vin = 5.0, R1 = 1 kΩ, R2 = 3 kΩ, σVin = 0.01, σR1 = 1.0, σR2 = 1.0:\n\nVout_num = Symbolics.substitute(\n    Vout,\n    Dict(Vin => 5.0, R1 => 1_000.0, R2 => 3_000.0,\n         σVin => 0.01, σR1 => 1.0, σR2 => 1.0),\n)\n# Vout_num.val ≈ 3.75 V\n# Vout_num.err ≈ 0.00987…  (combined from all three σ)\n\nVerified across test/examples/test_voltage_divider.jl, test_budget_voltage_divider.jl, and test_substitute_voltage_divider.jl.\n\n","title":"2. Voltage divider (sensitivity analysis)"},{"category":"section","location":"worked-examples/#3.-RC-time-constant-(τ-R-·-C)","page":"Worked Examples","text":"Setup. Resistance R ± σR and capacitance C ± σC. Measurand: first-order time constant τ = R · C.\n\nusing SymbolicUncertainties, Symbolics\nusing SymbolicUncertainties: ±\n\n@variables R C σR σC\n\nτ = (R ± σR) * (C ± σC)\n# τ.val = R * C\n# τ.err = sqrt((σR·C)² + (R·σC)²)\n\nPropagation. Product of two independent inputs — sensitivity coefficients ∂τ/∂R = C and ∂τ/∂C = R. Combined standard uncertainty is τ · sqrt((σR/R)² + (σC/C)²) after simplification.\n\nUncertainty budget.\n\nuncertainty_budget(τ, [R, C], [σR, σC])\n\nNumeric result. R = 1 kΩ, C = 1 µF, σR = 10 Ω, σC = 10 nF:\n\nτ_num = Symbolics.substitute(\n    τ,\n    Dict(R => 1_000.0, C => 1e-6, σR => 10.0, σC => 1e-8),\n)\n# τ_num.val = 1e-3 s (= 1 ms)\n# τ_num.err ≈ sqrt((10·1e-6)² + (1000·1e-8)²)\n#           = sqrt(1e-10 + 1e-10) ≈ 1.414e-5 s\n\n","title":"3. RC time constant (τ = R · C)"},{"category":"section","location":"worked-examples/#4.-Dissipated-power-(P-V-·-I-or-P-V-/-R)","page":"Worked Examples","text":"Setup. Voltage V ± σV and current I ± σI. Measurand: dissipated power P = V · I.\n\nusing SymbolicUncertainties, Symbolics\nusing SymbolicUncertainties: ±\n\n@variables V I σV σI\n\nP = (V ± σV) * (I ± σI)\n# P.val = V * I\n# P.err = sqrt((σV·I)² + (V·σI)²)\n\nPropagation. Product of two independent inputs.\n\nAlternative form using the Ohm's-law substitution I = V / R:\n\n@variables R σR\nP_alt = propagate(\n    (v, r) -> v^2 / r,\n    [V ± σV, R ± σR],\n)\n# P_alt.val = V² / R\n# P_alt.err = sqrt((2V/R · σV)² + (V²/R² · σR)²)\n\nThe two forms give different propagated uncertainties because they derive from distinct measurement models — a reminder that the GUM budget depends on which measurements are taken, not on the physical quantity itself.\n\nNumeric result. V = 5.0, I = 0.5, σV = 0.01, σI = 0.001:\n\nP_num = Symbolics.substitute(\n    P,\n    Dict(V => 5.0, I => 0.5, σV => 0.01, σI => 0.001),\n)\n# P_num.val = 2.5 W\n# P_num.err ≈ sqrt((0.01·0.5)² + (5·0.001)²)\n#           = sqrt(2.5e-5 + 2.5e-5) ≈ 0.00707 W\n\n","title":"4. Dissipated power (P = V · I or P = V² / R)"},{"category":"section","location":"worked-examples/#5.-RLC-resonance-(f-1-/-(2π-(LC)))","page":"Worked Examples","text":"Setup. Inductance L ± σL and capacitance C ± σC. Measurand: resonant frequency f₀ = 1 / (2π · sqrt(L · C)).\n\nusing SymbolicUncertainties, Symbolics\nusing SymbolicUncertainties: ±\n\n@variables L C σL σC\n\nf₀ = propagate(\n    (l, c) -> 1 / (2π * sqrt(l * c)),\n    [L ± σL, C ± σC],\n)\n# f₀.val = 1 / (2π · sqrt(L·C))\n# f₀.err = f₀.val/2 · sqrt((σL/L)² + (σC/C)²)\n\nPropagation. propagate handles the sqrt composition symbolically. Sensitivity coefficients are ∂f₀/∂L = -f₀/(2L) and ∂f₀/∂C = -f₀/(2C), giving the half-logarithmic-derivative combined form shown above.\n\nInverse inference — protocol optimisation (M5).\n\n# What σL is needed so u(f₀) ≤ 0.001·f₀.val ?\nrequired = infer_precision(f₀, σL, 0.001 * f₀.val)\n# Closed-form symbolic expression in (L, C, σC) —\n# see test/examples/test_rlc_resonance.jl (M5 exit gate).\n\nNumeric result. L = 10 mH, C = 1 µF, σL = 50 µH, σC = 5 nF:\n\nf₀_num = Symbolics.substitute(\n    f₀,\n    Dict(L => 10e-3, C => 1e-6, σL => 50e-6, σC => 5e-9),\n)\n# f₀_num.val ≈ 1591.55 Hz\n# f₀_num.err ≈ 5.59 Hz  (combined from L and C contributions)\n\nVerified in test/examples/test_rlc_resonance.jl.\n\n","title":"5. RLC resonance (f₀ = 1 / (2π √(LC)))"},{"category":"section","location":"worked-examples/#6.-RC-charge-ODE-(du/dt-(Vin-u)-/-(R·C))","page":"Worked Examples","text":"Setup. A first-order dynamic measurement — the standard RC charge transient. Parameters R ± σR, C ± σC, Vin ± σVin are treated as constants with uncertainty. This example requires the ModelingToolkit.jl package extension (M9).\n\nusing ModelingToolkit\nusing ModelingToolkit: t_nounits as t, D_nounits as D, System\nusing SymbolicUncertainties\nusing SymbolicUncertainties: ±\n\n@variables u(t)\n@parameters R C Vin\neqs = [D(u) ~ (Vin - u) / (R * C)]\n@named rc_sys = System(eqs, t)\n\nR_m   = R   ± 0.01R\nC_m   = C   ± 0.01C\nVin_m = Vin ± 0.001Vin\n\nSymbolic snapshot via propagate_ode:\n\nresult = propagate_ode(rc_sys, [R_m, C_m, Vin_m])\n# result[1].val = u(t)\n# result[1].err = sqrt((∂u/∂R)²·σR² + (∂u/∂C)²·σC² +\n#                      (∂u/∂Vin)²·σVin²)\n\nAugmented integration via uncertainty_ode:\n\nusing OrdinaryDiffEq\n\naugmented = uncertainty_ode(rc_sys, [R_m, C_m, Vin_m])\ncompiled  = mtkcompile(augmented)\n\nu0 = Dict(u => 0.0,\n          (s => 0.0 for s in setdiff(unknowns(augmented),\n                                     unknowns(rc_sys)))...)\np  = Dict(R => 1.0, C => 1e-3, Vin => 5.0)\nprob = ODEProblem(compiled, merge(u0, p), (0.0, 50e-3))\nsol  = solve(prob, Tsit5(); abstol = 1e-10, reltol = 1e-10)\n\n# sol[u, end] ≈ 5.0  (converged to Vin)\n# sol[∂u_∂Vin(t), end] ≈ 1.0\n\nVerified in test/ext_modelingtoolkit/test_rc_charge_endpoint.jl (M9 SC-003 exit gate). For a full walkthrough of the two M9 functions (propagate_ode and uncertainty_ode), see ODE Integration.\n\n","title":"6. RC-charge ODE (du/dt = (Vin − u) / (R·C))"},{"category":"section","location":"worked-examples/#Refresh-procedure-(when-Symbolics-output-drifts)","page":"Worked Examples","text":"These examples are backed by dedicated test items in test/examples/ and test/ext_modelingtoolkit/. If a future Symbolics.jl release changes a user-visible output format, the affected example's narrative may become out of date. Procedure:\n\nRun the corresponding test file — julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->ti.filename == \"…\"'.\nIf the test still passes, no change is needed — the code block in this page is still valid.\nIf the test fails on output drift, update the test to the new baseline, then mirror the same update in this page.\n\nThe test files are authoritative; this page consolidates them for reader convenience.","title":"Refresh procedure (when Symbolics output drifts)"},{"category":"section","location":"protocol-optimisation/#Protocol-Optimisation","page":"Protocol Optimisation","text":"The protocol-optimisation layer turns the GUM forward- propagation pipeline into a design tool: answer the precision-requirement question and the budget-allocation question in closed form.\n\nTwo operations:\n\nrequired_precision(m, σᵢ, target_uc) — the analytical \"precision condition\" on σᵢ to meet a target combined standard uncertainty (EARS REQ-060).\nbudget_allocation(m, variables, sigmas, total_budget) — the Lagrange-multiplier optimal allocation of a fixed total budget across inputs (REQ-061).","title":"Protocol Optimisation"},{"category":"section","location":"protocol-optimisation/#required_precision-—-inequality-framing","page":"Protocol Optimisation","text":"using Symbolics, SymbolicUncertainties\n\n@variables V I σV σI\nR = (V ± σV) / (I ± σI)\n\ncond = required_precision(R, σV, 0.01)\n\nRead the returned Num as the boundary value: σV values strictly below satisfy m.err < target_uc; values equal saturate; values above violate.\n\nrequired_precision is implemented as a direct delegate to infer_precision — see specs/007-protocol-and-inference/research.md R6 for the rationale. Both functions return the same Num expression; the distinct names preserve the two EARS framings (precision condition REQ-060 vs. solved equation REQ-090).","title":"required_precision — inequality framing"},{"category":"section","location":"protocol-optimisation/#budget_allocation-—-Lagrange-optimal-distribution","page":"Protocol Optimisation","text":"Given a fixed total uncertainty budget the measurement can afford, budget_allocation returns the per-input σᵢ* that minimises m.err subject to Σᵢ σᵢ = total_budget. The closed form is the inverse-sensitivity-squared weighting:\n\nσᵢ* = total_budget · (1 / cᵢ²) / Σⱼ (1 / cⱼ²)\n\nwhere cᵢ = sensitivity_coefficient(m, xᵢ).","title":"budget_allocation — Lagrange-optimal distribution"},{"category":"section","location":"protocol-optimisation/#Symmetric-example","page":"Protocol Optimisation","text":"For a pure sum m = a + b + c with total budget B, all three sensitivity coefficients are 1, so every input gets B/3 — the symmetric optimum:\n\n@variables a σa b σb c σc\nm = propagate((x, y, z) -> x + y + z, [a ± σa, b ± σb, c ± σc])\n\nalloc = budget_allocation(m, [a, b, c], [σa, σb, σc], 1.0)\n# alloc[σa] = alloc[σb] = alloc[σc] = 1/3 after numeric\n# evaluation.","title":"Symmetric example"},{"category":"section","location":"protocol-optimisation/#Asymmetric-example","page":"Protocol Optimisation","text":"For m = 2a + 3b the sensitivity coefficients are c_a = 2, c_b = 3, so the inverse-squared weighting is 1/4 : 1/9 ⇒ the σ allocation ratio is 9 : 4. This is the stationarity condition cᵢ²·σᵢ = cⱼ²·σⱼ.\n\nalloc = budget_allocation(\n    propagate((x, y) -> 2x + 3y, [a ± σa, b ± σb]),\n    [a, b], [σa, σb], 1.0,\n)\n# alloc[σa] ≈ 9/13,  alloc[σb] ≈ 4/13.","title":"Asymmetric example"},{"category":"section","location":"protocol-optimisation/#Error-paths","page":"Protocol Optimisation","text":"length(variables) != length(sigmas) → DimensionMismatch.\nNumeric total_budget < 0 → ArgumentError.\nEvery sensitivity coefficient zero (measurand independent of every supplied input) → ArgumentError per REQ-091.\n\nPer-variable sensitivity_coefficient failures (e.g. the M2 REQ-021 unresolved-differential path) propagate unchanged.","title":"Error paths"},{"category":"section","location":"protocol-optimisation/#API-reference","page":"Protocol Optimisation","text":"","title":"API reference"},{"category":"function","location":"protocol-optimisation/#SymbolicUncertainties.required_precision","page":"Protocol Optimisation","text":"required_precision(m::SymbolicMeasurement, σᵢ, target_uc) -> Num\n\nnote: Delegate\nDirect delegate to infer_precision. Both return the closed-form σᵢ* that saturates m.err = target_uc. The EARS REQ-060 name preserves the \"precision condition\" (inequality) framing; the REQ-090 name preserves the \"solved equation\" framing. The numerical content is the same; see specs/007-protocol-and-inference/research.md R6 for the rationale.\n\nUsers who want the inequality interpretation read the returned expression as the boundary value: σᵢ values strictly below satisfy m.err < target_uc; values equal saturate it; values above violate it. Inverse form of the JCGM 100:2008 §5.2 uncertainty-propagation relation.\n\nTraces REQ-060, REQ-062.\n\n\n\n\n\n","title":"SymbolicUncertainties.required_precision"},{"category":"function","location":"protocol-optimisation/#SymbolicUncertainties.budget_allocation","page":"Protocol Optimisation","text":"budget_allocation(m, variables, sigmas, total_budget) -> Dict{Num, Num}\n\nReturn the Lagrange-multiplier optimal allocation of a fixed total uncertainty budget across inputs: the σᵢ* values that minimise m.err subject to the linear constraint Σᵢ σᵢ = total_budget.\n\nUses the closed-form inverse-sensitivity-squared weighting (research R5):\n\nσᵢ* = total_budget · (1 / cᵢ²) / Σⱼ (1 / cⱼ²)\n\nwhere cᵢ = sensitivity_coefficient(m, xᵢ).\n\nErrors:\n\nlength(variables) != length(sigmas) → DimensionMismatch.\nNumeric total_budget < 0 → ArgumentError.\nEvery cᵢ symbolically zero (the measurand is independent of every supplied input) → ArgumentError per REQ-091.\nPer-variable sensitivity_coefficient failure propagates the standard M2 REQ-021 ArgumentError.\n\nInvariant: summing the returned values and simplifying gives total_budget (the budget constraint).\n\nImplements the methodology of JCGM 100:2008 §5.2.2 squared and minimised under the linear budget constraint. Traces REQ-061, REQ-062.\n\n\n\n\n\n","title":"SymbolicUncertainties.budget_allocation"},{"category":"section","location":"#SymbolicUncertainties.jl","page":"Home","text":"A Julia package for purely symbolic propagation of measurement uncertainties, implementing the methodology of JCGM 100:2008 (GUM — Guide to the Expression of Uncertainty in Measurement) and its supplements, built on Symbolics.jl.\n\nwarning: Regulated-use disclaimer\nThis package is intended for research and educational purposes. It implements the mathematical methodology of JCGM 100:2008 but has not been independently validated as calibration software for accreditation use. Use in regulated, safety-critical, or ISO/IEC 17025-accreditation contexts requires independent software validation per ISO/IEC 17025 §6.4.7. See the Limitations page and LICENSE.md for the full non-warranty clause.","title":"SymbolicUncertainties.jl"},{"category":"section","location":"#Status-—-Milestone-M10-(v0.10.0,-pre-1.0)","page":"Home","text":"The public API is stabilised at 25 exports: symbolic uncertainty propagation (propagate, propagate_vector, ±), sensitivity analysis (sensitivity_coefficient, uncertainty_contribution, relative_sensitivity), uncertainty budgets (uncertainty_budget, expanded_uncertainty, welch_satterthwaite, dominant_source), precision inference (infer_precision, infer_all_precisions, required_precision, budget_allocation), a linearity diagnostic (check_linearity), code generation (build_evaluator, to_expr, latex, JuliaTarget, CTarget), and ModelingToolkit.jl integration (propagate_ode, uncertainty_ode).\n\nStart with Getting Started; the Methodology Reference maps every export to its JCGM 100:2008 section.\n\nThe next milestone, M11, is a breaking foundation refactor: an uncertain quantity will carry the independent sources it derives from, so x - x returns 0 ± 0 through the binary operators and correlation becomes a property of the sources rather than a user-supplied covariance matrix. See the project ROADMAP.md.","title":"Status — Milestone M10 (v0.10.0, pre-1.0)"},{"category":"section","location":"#Contributor-quickstart","page":"Home","text":"using Pkg\nPkg.activate(\".\")\nPkg.instantiate()\nPkg.test()\n\nRequires Julia ≥ 1.10 (LTS).","title":"Contributor quickstart"},{"category":"section","location":"#Normative-references","page":"Home","text":"Standard Scope\nJCGM 100:2008 (GUM) Linear uncertainty propagation, uncertainty budget\nJCGM 101:2008 (GUM S1) Monte Carlo propagation — out of scope (see MonteCarloMeasurements.jl)\nJCGM 102:2011 (GUM S2) Multiple output quantities\nJCGM 104:2009 Introduction to GUM — terminology\nIEC 60359 Electrical instrument performance\nIEC 61298-3 Process measurement uncertainty\nEA-4/02 Calibration uncertainty expression","title":"Normative references"}]
}
